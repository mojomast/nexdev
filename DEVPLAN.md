# Nexdev Development Plan

**Status:** M0 bootstrap complete; implementation base imported  
**Canonical spec:** `SPEC.md`  
**Generated by:** Nexdev Build Orchestrator planning session  
**Date:** 2026-06-29  
**Execution model for later build session:** subagent-only implementation under orchestrator control

## 0. Planning Session Outcome

This repository now contains the canonical Nexdev specification, planning artifacts, and an imported geoffrussy Go base for M0. Follow-on work must continue under orchestrator control and use builder subagents for implementation changes.

This plan intentionally does not scaffold Go packages or write production code. It converts `SPEC.md` into an implementation-ready parallel-development plan with stable ownership boundaries, contracts-first sequencing, explicit testing, documentation obligations, spec-management rules, and blocker escalation.

## 1. Scout Reports

### 1.1 Spec Scout Report

Canonical spec:
- `SPEC.md` is the only discovered spec candidate and is canonical.
- Title: `Nexdev v0.1 Implementation Specification`.
- Target: Go 1.24+, fork `mojomast/geoffrussy`, preserve the Ussyverse thesis.
- The spec explicitly states that implementation agents must not use source internals as implicit contract. The spec is the contract.

Required product summary:
- Build a local-first, single-binary, Go-first coding harness.
- Preserve geoffrussy foundations: provider registry, SQLite state, navigation, executor, git, CLI/TUI stack.
- Port devussy concepts: pre-development pipeline, interview, complexity, design correction, sanity review, HiveMind, staged devplan generation, checkpointed artifacts, handoff.
- Port nexussy concepts: live control plane, persisted SSE replay, MCP-compatible tools, pause/resume/skip/cancel/steer/detour controls, worker orchestration ideas, deployment profiles.
- Do not port Python service layout, Python subprocess model, role YAML files, or AgentRouter-specific tricks.

Required modules:
- `cmd/nexdev`
- `internal/app`
- `internal/config`
- `internal/controlplane`
- `internal/contract`
- `internal/pipeline`
- `internal/executor`
- `internal/detour`
- `internal/provider`
- `internal/state`
- `internal/safety`
- `internal/git`
- `internal/observability`
- `internal/tui`
- Optional `web/static`
- `api/openapi.yaml`
- `api/mcp_tools.json`
- `scripts/*`
- `docs/*`

Required runtime modes:
- `nexdev run`
- `nexdev run --no-tui --json`
- `nexdev develop`
- `nexdev serve`
- `nexdev run --stage validate`
- Resume through `nexdev run`
- `nexdev doctor`

Required stage graph:
- `init`
- `repo_analyze`
- `interview`
- `complexity`
- `design`
- `hivemind`
- `validate`
- `plan_sketch`
- `plan_detail`
- `review`
- `develop`
- `verify`
- `handoff`
- `complete`
- `detour` is a pseudo-stage reachable only from `develop` and returns to `develop`.

Required control-plane routes:
- `GET /health`
- `GET /status`
- `GET /plan`
- `GET /artifacts`
- `GET /events`
- `GET /runs/{run_id}/stream`
- `POST /runs`
- `POST /pause`
- `POST /resume`
- `POST /skip`
- `POST /steer`
- `POST /detour`
- `POST /cancel`
- `PUT /tasks/{task_id}`
- `DELETE /tasks/{task_id}`
- `POST /blockers/{blocker_id}/resolve`
- `GET /config`
- `PUT /config`
- `GET /providers`
- `POST /providers/{name}/test`
- `GET /mcp/tools`
- `POST /mcp/call`

Required state tables from the spec:
- Preserve existing geoffrussy tables where possible after the fork.
- Add `runs`.
- Add `stage_runs`.
- Add `events` with `(run_id, sequence)` uniqueness.
- Add `artifacts`.
- Add `hivemind_results`.
- Add `validate_results`.
- Add `steering_events`.
- Add `detour_records`.
- Add `navigation_events`.
- Add `plan_edit_events`.
- Add `auth_tokens`.
- Add audit/security tables if needed by implementation details.

Required CLI commands:
- `nexdev init [--name NAME] [--description DESC]`
- `nexdev init --import-devussy PATH`
- `nexdev run [--from STAGE] [--stage STAGE] [--yes] [--cheap] [--brrrr]`
- `nexdev develop`
- `nexdev verify`
- `nexdev status [--json]`
- `nexdev plan [--json]`
- `nexdev review`
- `nexdev navigate <stage>`
- `nexdev detour [--reason TEXT]`
- `nexdev steer "message"`
- `nexdev pause [--reason TEXT]`
- `nexdev resume`
- `nexdev cancel [--reason TEXT]`
- `nexdev blockers list|resolve`
- `nexdev provider list`
- `nexdev provider test <name>`
- `nexdev events [--follow]`
- `nexdev artifacts list|open`
- `nexdev history`
- `nexdev config print|validate|set`
- `nexdev auth token create|list|revoke`
- `nexdev serve`
- `nexdev doctor`

Required global CLI flags:
- `--project-dir DIR`
- `--config FILE`
- `--state-dir DIR`
- `--no-tui`
- `--json`
- `--log-level debug|info|warn|error`
- `--profile dev|trusted-lan|ci`
- `--control-url URL`
- `--token TOKEN`

Required security guarantees:
- Local-first by default.
- Bind to `127.0.0.1` by default.
- Fail startup if binding non-loopback without auth.
- Deny untrusted generated commands unless explicit tool policy allows them.
- Treat repo files, docs, test fixtures, MCP descriptions, issue text, and model output as untrusted.
- Enforce path sanitizer, symlink escape checks, file lock checks, and policy checks before writes.
- Scrub provider API keys, bearer tokens, passwords, private keys, SSH keys, `.env` values, and known secret patterns from logs, events, artifacts, prompts, and error reports.
- Persist every event before broadcast.
- Schema-validate every model-generated structured artifact and repair or reject before use.
- Record provider name, model name, latency, retries, token usage, cost estimates, and redacted errors.
- Include deterministic fake-provider and fake-worker modes for CI.

Required test families:
- Unit tests for config, path sanitizer, redaction, roles, event replay, SSE formatting, stage prerequisites, plan DAG validation, detour splicing, steering summarization, structured-output repair, and SQL migrations.
- Integration tests for fake-provider full pipeline, invalid JSON repair, pause/resume/skip, SSE reconnect, auth matrix, blocker-detour-resume, plan edit versioning, verify repair, and geoffrussy migration.
- E2E fake-provider script that initializes a temp repo, runs full pipeline, connects SSE, verifies replay, verifies artifacts, verifies changed files, and reaches `complete`.
- Security tests for prompt injection, malicious `AGENTS.md`, symlink escapes, `.env` exfiltration, role mutation, unbounded output, slow SSE client, and MCP tool poisoning.

Spec scout decision gates:
- Decide exact module path during bootstrap.
- Decide whether auth tokens are project-local only for v0.1 or also global.
- Decide exact `auth_required: auto` resolution.
- Decide generated API package name and import boundaries.
- Decide whether `sqlc` and `goose` are adopted immediately or after geoffrussy inspection.
- Decide how duplicated `develop.commit_on_*` and `git.commit_on_*` config precedence resolves.
- Decide whether v0.1 includes only worktree-worker scaffolding or active parallel task execution.

### 1.2 Repo Scout Report

Repository identity:
- Path: `/home/mojo/projects/nexdev`.
- Current state: M0 bootstrap imported `mojomast/geoffrussy` source into the existing Nexdev repository while preserving planning artifacts.
- Git is initialized with `origin https://github.com/mojomast/nexdev.git`.
- Module path is `github.com/mojomast/nexdev` with Go 1.24 toolchain metadata inherited from geoffrussy.
- Existing files at planning start: `SPEC.md` plus planning docs from the planning session.

Missing implementation structure:
- `go.mod` and `go.sum` now exist.
- `cmd/nexdev/main.go` now exists as a minimal wrapper over imported CLI wiring.
- Imported geoffrussy `internal/` packages now exist, including provider, state, navigation, executor, git, CLI/TUI dependencies, and tests.
- No `api/openapi.yaml`.
- Imported geoffrussy tests exist and pass under the Nexdev module path.
- No CI.
- No scripts.
- Planning docs remain present and `docs/architecture.md` records the base strategy.

Repository readiness conclusion:
- M0 imported geoffrussy from `https://github.com/mojomast/geoffrussy.git` at `e29f8e7649584585a93d8fc8ac9123036fcaf38e`.
- The module was renamed to `github.com/mojomast/nexdev` and all Go import paths were updated.
- Root planning artifacts were preserved: `SPEC.md`, `DEVPLAN.md`, `AGENTS.md`, `WORKER_PROTOCOL.md`, `SPEC_UPDATE_PROTOCOL.md`, `TESTING_STRATEGY.md`, `PROMPT_FOR_DEVELOPMENT_SESSION.md`, `docs/architecture.md`, `docs/contracts.md`, and `README.md`.
- Upstream geoffrussy failed baseline tests before import because `internal/design` tests referenced missing `extractJSONFromMarkdown`; M0 added the minimal helper after import so the baseline compiles and passes.

Recommended bootstrap safeguards:
- `SPEC.md` remains untouched and canonical.
- `.gitignore` includes Nexdev runtime state, imported geoffrussy runtime state, logs, temp DB/build artifacts, workspace files, local env files, and common editor/OS artifacts.
- M1 must establish `api/openapi.yaml` and core contracts before feature workers begin.

### 1.3 Best-Practice Scout Report

Adopt these practices unless geoffrussy inspection proves a smaller compatible path exists:
- Use Cobra for CLI if geoffrussy already uses it.
- Use standard `net/http` `ServeMux` with Go 1.22+ method/path patterns.
- Use explicit HTTP server timeouts and context cancellation.
- Use persisted SSE with `Last-Event-ID`, heartbeat, bounded queues, replay query, and no `[DONE]` sentinel.
- Use SQLite WAL, `foreign_keys=ON`, `busy_timeout`, bounded transactions, retry on `SQLITE_BUSY`, and conservative connection pooling.
- Keep `github.com/mattn/go-sqlite3` initially if inherited from geoffrussy.
- Adopt `sqlc` for durable state queries unless geoffrussy already has a small typed query layer that is easier to preserve.
- Keep geoffrussy migration runner if solid; otherwise adopt `github.com/pressly/goose/v3` with embedded SQL migrations.
- Make `api/openapi.yaml` authoritative and use `oapi-codegen` for Go types/server glue.
- Use `kin-openapi` in tests and dev validation for contract drift.
- Use standard `log/slog` with a redacting handler.
- Use OpenTelemetry API and enable SDK/exporters only under config.
- Use opaque bearer tokens by default, hashed in SQLite with role, expiry, revocation, and constant-time comparison.
- Defer JWS/JWT unless a real stateless-token requirement appears.
- Make MCP tools thin wrappers around control-plane service methods and the same role checks.
- Use deterministic fake providers and fake workers for CI.
- Default to single worktree for v0.1 execution; enable parallel coding only with explicit worktree isolation and non-overlapping file ownership.

Reject or defer:
- Reject heavy web frameworks for v0.1.
- Reject ORMs.
- Reject Redis/NATS/Temporal-style queue frameworks.
- Reject homegrown JWT.
- Defer CGO-free SQLite unless release requirements demand it.
- Defer heavy OS sandboxing until the policy-gated command runner and sandbox interface are stable.

### 1.4 Contract Scout Report

Contracts that must freeze before broad parallel work:
- OpenAPI route schemas and error responses.
- Event envelope, event types, sources, replay semantics, and SSE frame shape.
- State migrations and typed state models.
- Stage names, statuses, canonical stage order, and prerequisite rules.
- Structured model output schemas.
- Provider interface, router slots, structured output wrapper, usage metadata.
- Executor update/event mapping, task status, blocker shape, control methods.
- Detour request/result/splice/depth contracts.
- Steering message/source/summary/context-window contracts.
- Artifact kinds, manifests, changed-file schema, verify report, run summary.
- Test fixtures and fake provider behavior.

Contract ownership rules:
- `internal/state` owns durable persistence and must not import pipeline, controlplane, executor, detour, TUI, or provider implementations.
- `api/openapi.yaml` owns public HTTP wire contracts.
- Generated API types may be imported at control-plane/client boundaries, not deep domain packages.
- `internal/provider` owns model-provider boundaries; stages import provider interfaces/router only.
- `internal/pipeline` owns stage graph and prerequisite logic.
- `internal/controlplane` owns HTTP/SSE/MCP adapters and auth enforcement.
- `internal/executor` owns task execution contracts and task updates.
- `internal/detour` owns detour generation and plan splicing.
- Shared test fixtures are test-only.

Contract risks:
- Import cycles from overly concrete `StageEnv` references.
- Central `internal/contract` becoming a dumping ground.
- Event payload ambiguity if payload structs are not defined early.
- Stage/task status duplication across state, API, executor, and events.
- Provider direct calls bypassing router/cost/audit controls.
- Auth route role drift between OpenAPI, middleware, MCP, and CLI remote mode.
- Detour and review plan mutation diverging in version/audit behavior.

### 1.5 Testing Scout Report

Current baseline:
- No runnable test commands exist yet because the repo has no Go module.
- All commands below become required after bootstrap unless marked recommended.

Required commands after implementation exists:
- `go test ./...`
- `go vet ./...`
- `govulncheck ./...`
- `go test -race ./...`
- `go mod verify`
- `./scripts/e2e_fake_provider.sh`
- `nexdev doctor`
- `nexdev config validate`
- `nexdev run --fake-provider --no-tui --json`

Recommended additions:
- `make test`
- `make vet`
- `make race`
- `make vuln`
- `make contract`
- `make smoke`
- `make ci`
- `scripts/smoke.sh`
- `scripts/real_provider_smoke.sh`, gated by env and spend cap

Shared fixtures to create early:
- `TempProject(t)` minimal repo workspace.
- `TempSQLiteStore(t)` with migrations applied.
- `SeedGeoffrussyState(t, db)` for additive migration tests.
- `FakeClock` and `FakeIDGenerator`.
- `FakeProvider` with scripted stage responses, invalid JSON, repairs, usage, latency, and errors.
- `FakeWorker` with scripted task updates, file writes, blockers, and verify failures.
- `EventRecorder`.
- SSE test client supporting reconnect and `Last-Event-ID`.
- Auth token helper creating observer/operator/admin tokens through public APIs.
- Golden-file helper gated by explicit update env var.
- Security fixture repos for prompt injection, malicious `AGENTS.md`, symlink escape, and fake `.env`.
- OpenAPI validation helper.

### 1.6 Docs/Spec Scout Report

Existing docs:
- `SPEC.md` only.

Required durable docs:
- `README.md`, later when the implementation has user-facing quickstart.
- `DEVPLAN.md`.
- `AGENTS.md`.
- `WORKER_PROTOCOL.md`.
- `SPEC_UPDATE_PROTOCOL.md`.
- `TESTING_STRATEGY.md`.
- `docs/architecture.md`.
- `docs/contracts.md`.
- `docs/SECURITY.md`, later by security milestone.
- `docs/API.md`, later after OpenAPI exists.
- `docs/configuration.md`, later after config behavior exists.
- `api/openapi.yaml`, first-wave contract.

Documentation rule:
- Do not duplicate full spec prose in secondary docs.
- Operational docs summarize implementation-facing rules and link back to `SPEC.md`.
- Workers update docs in the same milestone as behavior changes.
- Spec-management subagent updates `SPEC.md` after every major milestone only to reflect accepted implemented behavior, clarifications, or approved deviations.

## 2. Project Summary

Nexdev builds a next-generation local coding harness. It synthesizes:
- Geoffrussy as the Go foundation: provider registry, SQLite state, navigation, executor, git, path safety, CLI/TUI dependencies, and migrations.
- Devussy as the pre-development pipeline: adaptive interview, complexity profiling, architecture/design generation, design correction, HiveMind critique, validation, two-tier devplan generation, checkpointed artifacts, and handoff.
- Nexussy as the control-plane model: live HTTP/SSE control, MCP-compatible tool surface, permissions, steering/injection, blocker management, worker orchestration concepts, artifacts, and deployment profiles.

Nexdev is not a chat application. It is a local-first software delivery harness that drives a full lifecycle:
- Ideation.
- Interview.
- Repository analysis.
- Complexity assessment.
- Design.
- HiveMind critique.
- Validation.
- Plan sketch.
- Detailed plan.
- Review.
- Develop.
- Verify.
- Handoff.

The preferred architecture is a Go-first single binary unless geoffrussy inspection proves a narrower migration path. The binary exposes CLI, optional TUI, HTTP/SSE, and MCP-compatible surfaces over the same core services and SQLite state.

Core characteristics:
- Local-first by default.
- Safe-by-default tool execution.
- SQLite-backed durable run state.
- Persisted SSE event bus with replay.
- Opaque token auth and role enforcement.
- Provider registry and per-stage provider routing.
- Structured model output validation and repair.
- Detour/deblocker workflow for blocked work.
- Durable steering context.
- Anchored artifacts and changed-file manifests.
- Deterministic fake-provider/fake-worker CI modes.
- Subagent-friendly implementation with strict file ownership and contracts.

Human-facing docs may preserve Ussyverse energy. Machine contracts must remain boring, typed, stable, versioned, and testable.

## 3. Source-Of-Truth Hierarchy

Precedence order:

1. Canonical Nexdev spec: `SPEC.md`.
2. Generated and documented contracts: `api/openapi.yaml`, generated API types, `docs/contracts.md`, state migrations, JSON schemas.
3. `DEVPLAN.md`.
4. Test fixtures, golden files, and schemas.
5. Implementation.

Rule:
- If implementation disagrees with `SPEC.md`, the implementation is wrong unless the spec-management subagent has updated `SPEC.md` with explicit orchestrator approval.

Contract drift rule:
- If `api/openapi.yaml`, migrations, generated types, fixture schemas, and docs disagree, workers must stop and escalate a contract mismatch blocker.

Spec update rule:
- Workers do not silently weaken requirements.
- Workers record proposed spec changes in their handoff.
- The orchestrator spawns a spec-management subagent after each major milestone.
- Spec-management may update `SPEC.md` only to reflect accepted implemented behavior, approved deviations, clarifications, or explicit deferrals.

## 4. Repository Bootstrap Decision Gate

Current repo state is greenfield planning-only. Before implementation, the development-session orchestrator must choose one path.

Default decision:
- Fork or import `mojomast/geoffrussy` into this repository, preserving root `SPEC.md` and this plan.

Alternative decision:
- Start greenfield only if geoffrussy is unavailable or incompatible, and record the deviation in `SPEC.md` through spec management before implementing foundations that the spec expects from geoffrussy.

Bootstrap acceptance:
- Repository has Git initialized or imported.
- `SPEC.md` remains present and canonical.
- `DEVPLAN.md`, protocols, and docs remain present.
- Go module exists with approved module path.
- Initial tests compile.
- No production feature work begins before M1 contracts.

## 5. Parallel Work Architecture

Each domain below has a primary owner subagent role. Workers may read `SPEC.md`, `DEVPLAN.md`, `WORKER_PROTOCOL.md`, `SPEC_UPDATE_PROTOCOL.md`, `TESTING_STRATEGY.md`, and relevant contract docs. Workers may edit only their owned paths plus explicitly allowed shared contract files listed in their task.

### 5.1 Foundation / Module Setup

Owner subagent role:
- Foundation Worker.

Primary goal:
- Establish the geoffrussy fork/import or approved greenfield module without implementing product features.

Owned files:
- `go.mod`
- `go.sum`
- `.gitignore`
- `.github/workflows/*`
- `cmd/nexdev/main.go` only for minimal compile stub after contracts allow it
- `internal/app/*` for dependency wiring skeleton only
- Build/tool files such as `Makefile`, `tools.go`, or Go 1.24 tool declarations

Inputs:
- `SPEC.md`
- `DEVPLAN.md`
- Repo scout report
- Orchestrator module-path decision

Outputs:
- Go module initialized.
- Fork/import decision recorded.
- Baseline test command exists.
- Tooling pins for codegen/security tools are set.

Dependencies:
- None, but must run before code workers.

Parallelism:
- Low. One worker only because module/bootstrap files are shared.

Acceptance tests:
- `go test ./...` passes once module exists.
- `go vet ./...` passes if no packages are too incomplete.

Documentation obligations:
- Update `DEVPLAN.md` bootstrap status.
- Update `docs/architecture.md` with actual base strategy.

Spec obligations:
- If not using geoffrussy fork, escalate spec deviation before proceeding.

### 5.2 Contracts / API / Schema

Owner subagent role:
- Contract/API Worker.

Primary goal:
- Freeze first-wave machine contracts before feature workers begin.

Owned files:
- `api/openapi.yaml`
- `api/mcp_tools.json` once MCP starts
- `internal/contract/*`
- Generated API types under the chosen generated path
- Contract golden fixtures under contract-owned `testdata/`
- `docs/contracts.md`

Inputs:
- `SPEC.md` sections 8-15, 18, 20, 24
- Contract Scout report

Outputs:
- OpenAPI skeleton for all required routes.
- Error response schema.
- Event envelope schema.
- Structured artifact/model output structs or JSON schemas.
- Contract tests.

Dependencies:
- M0 bootstrap.

Parallelism:
- Medium after `api/openapi.yaml` skeleton is stable. Split API routes, event contracts, artifact schemas, and model output schemas only after file boundaries are assigned.

Acceptance tests:
- OpenAPI validates.
- Codegen compiles.
- Golden examples validate against schemas.
- Error/event/model-output tests pass.

Documentation obligations:
- Update `docs/contracts.md` for every contract.
- Update `TESTING_STRATEGY.md` for contract tests.

Spec obligations:
- Escalate route/schema ambiguity before implementation workers depend on it.

### 5.3 Config and Paths

Status: M2-CONFIG-PATHS verified on 2026-06-30 for typed defaults, validation baseline, and spec-target path sanitizer. Full loader precedence and executor policy integration remain follow-up work.

Evidence:
- `internal/config/nexdev.go` defines Nexdev typed defaults for profile, project state dir, control-plane bind/port/auth, security command/network defaults, tool policy path, repo-analyze excludes, and provider primary/stage placeholders while preserving imported geoffrussy config behavior.
- `internal/safety/paths.go` provides the M2 path sanitizer baseline for cleaned project-root writes, traversal/absolute escape rejection, `.git` denial, symlink escape rejection where filesystem state allows, and basic deny glob checks.
- `go test ./internal/config ./internal/safety` passed.

Next action guidance:
- CLI/app wiring should adopt `config.LoadNexdevYAML`, preserve imported geoffrussy compatibility until retired by an orchestrated task, and add global/project/env/flag precedence tests when loader wiring exists.
- Auth/security follow-up should extend validation for unsafe CORS/profile combinations and keep non-loopback auth behavior aligned with `controlplane.RequireAuthForBind` at app startup.
- Executor/tool-policy follow-up should add file lock and task expected-file checks on top of `internal/safety.PathSanitizer`.

Owner subagent role:
- Config/Safety Worker for config and path contracts.

Owned files:
- `internal/config/*`
- `internal/safety/paths.go`
- Config fixtures under `internal/config/testdata/`
- Path fixtures under `internal/safety/testdata/`
- `docs/configuration.md` when created

Inputs:
- Spec section 15 config schema
- Security path rules

Outputs:
- Typed config structs.
- Defaults.
- Loader precedence.
- Validation.
- Project/global path resolution.
- Project lock path contract.
- Path sanitizer and symlink checks.

Dependencies:
- M1 contract constants.

Parallelism:
- Medium. Config loader and path sanitizer can run in parallel if shared interfaces are frozen.

Acceptance tests:
- Config precedence tests.
- Unknown-key rejection.
- Profile validation.
- Remote bind without auth config rejection where appropriate.
- Path traversal and symlink escape tests.

Documentation obligations:
- Update `docs/architecture.md` for runtime paths.
- Create/update `docs/configuration.md` when config behavior exists.

Spec obligations:
- Resolve duplicate config precedence for `develop.commit_on_*` vs `git.commit_on_*`.

### 5.4 State and Migrations

Owner subagent role:
- State Worker.

Owned files:
- `internal/state/*`
- `internal/state/migrations/*`
- `internal/state/queries.sql`
- Generated sqlc files if adopted
- State test fixtures under `internal/state/testdata/`

Inputs:
- Spec sections 7, 8, 9, 12, 13, 14, 22, 23
- Contract Scout state model inventory

Outputs:
- SQLite store.
- WAL/foreign key/busy timeout setup.
- Additive migrations.
- Typed repositories for runs, stages, events, artifacts, auth tokens, steering, detours, navigation, plan edits.
- Migration tests.

Dependencies:
- M0 bootstrap.
- M1 contract enums and schemas.

Parallelism:
- Medium. Migration authoring, repository interfaces, and migration tests can be split after schema ownership is frozen.

Acceptance tests:
- Empty DB migrates to latest.
- Seeded geoffrussy-like DB migrates additively.
- Migrations are idempotent.
- Foreign keys enforced.
- WAL and busy timeout configured.
- Event sequence allocation is monotonic per run.

Documentation obligations:
- Update `docs/contracts.md` state schema section.
- Update `TESTING_STRATEGY.md` migration test matrix.

Spec obligations:
- Do not remove backward-compatible geoffrussy state unless orchestrator-approved spec update exists.

### 5.5 Provider Layer

Owner subagent role:
- Provider Worker.

Owned files:
- `internal/provider/*`
- Provider fake fixtures under `internal/provider/testdata/`

Inputs:
- Spec section 11
- Existing geoffrussy provider package after bootstrap

Outputs:
- Preserved provider registry.
- Per-stage provider router.
- Structured output wrapper.
- Usage/cost metadata contracts.
- Deterministic fake provider.

Dependencies:
- M1 model output schemas.
- M2 config.

Parallelism:
- Medium. Router, fake provider, structured wrapper, and usage metadata can be assigned separately after provider interfaces are frozen.

Acceptance tests:
- Provider slots inherit primary when empty.
- Structured output rejects unknown fields in strict mode.
- Repair attempts capped.
- Raw responses and validation errors persisted through state/audit hooks once state exists.
- Fake provider drives scripted stage responses.

Documentation obligations:
- Update `docs/architecture.md` provider flow.
- Update `TESTING_STRATEGY.md` fake-provider usage.

Spec obligations:
- No direct provider calls outside provider package or approved wrapper.

### 5.6 Pipeline Contracts and Runner

Owner subagent role:
- Pipeline Framework Worker.

Owned files:
- `internal/pipeline/stage.go`
- `internal/pipeline/runner.go`
- `internal/pipeline/navigation.go`
- `internal/pipeline/status.go`
- Pipeline framework tests

Inputs:
- Spec sections 9 and 10
- State contracts
- Event publisher interface

Outputs:
- Stage interface.
- Stage registry.
- Canonical stage order.
- Prerequisite validation.
- Status transitions.
- Resumption semantics.
- Stage checkpoint mechanism.

Dependencies:
- M1 contracts.
- M3 state repositories for durable status.

Parallelism:
- Low initially. One worker should own runner and navigation to avoid graph conflicts.

Acceptance tests:
- Stage graph matches spec.
- Invalid navigation rejected.
- Status transition tests pass.
- Resume selects persisted stage state.

Documentation obligations:
- Update `docs/architecture.md` stage graph.
- Update `docs/contracts.md` stage/status contracts.

Spec obligations:
- Any stage order change requires spec-management approval.

### 5.7 Pipeline Stages

Owner subagent role:
- Pipeline Stage Workers, one worker per stage group.

Stage groups:
- Repo Analysis Worker: `repo_analyze`.
- Interview/Complexity Worker: `interview`, `complexity`.
- Design Worker: `design` correction loop.
- Hivemind/Validation Worker: `hivemind`, `validate`.
- Planning Worker: `plan_sketch`, `plan_detail`.
- Review Worker: `review` gate and plan edit events.
- Verify/Handoff Worker: `verify`, `handoff`.

Owned files:
- `internal/pipeline/repo_analyze.go`
- `internal/pipeline/interview.go`
- `internal/pipeline/complexity.go`
- `internal/pipeline/design.go`
- `internal/pipeline/hivemind.go`
- `internal/pipeline/validate.go`
- `internal/pipeline/plan_sketch.go`
- `internal/pipeline/plan_detail.go`
- `internal/pipeline/review.go`
- `internal/pipeline/verify.go`
- `internal/pipeline/handoff.go`
- Stage-specific prompts under `internal/pipeline/prompts/*` if adopted
- Stage-specific tests and fixtures

Inputs:
- Pipeline runner.
- Provider structured wrapper.
- State repositories.
- Artifact contracts.
- Safety policy.

Outputs:
- Resumable stage implementations.
- Artifacts written and indexed.
- Structured validation and repair.
- Golden artifact tests.

Dependencies:
- M5 framework.
- M4 provider fake.
- M3 state.

Parallelism:
- High once runner, contracts, and fake provider exist. Each stage group uses non-overlapping files.

Acceptance tests:
- Stage-specific unit/integration tests.
- Fake provider completes pre-development pipeline.
- Artifacts match schema and are indexed.
- Invalid structured output follows repair/reject rules.

Documentation obligations:
- Update pipeline stage reference in `docs/architecture.md` or later `docs/pipeline.md` if created.
- Update `TESTING_STRATEGY.md` fixtures.

Spec obligations:
- Do not silently delete requirements to pass validation.

### 5.8 Executor Integration and Steering

Owner subagent role:
- Executor/Steering Worker.

Owned files:
- `internal/executor/*`
- `internal/steering/*` if separate package is adopted
- `internal/pipeline/develop.go`
- Executor tests and fake worker fixtures

Inputs:
- Geoffrussy executor after bootstrap
- Event contracts
- State task/blocker contracts
- Safety policy
- Provider router

Outputs:
- Extended task executor controls.
- Task update to event mapping.
- `CurrentTask` query.
- Context-aware pause/resume/cancel/skip.
- Steering context ingestion and prompt context builder.
- Fake worker develop path.

Dependencies:
- M1 contracts.
- M3 state.
- M4 provider.
- M5 pipeline runner.

Parallelism:
- Medium. Steering context and event mapping can split from executor controls after interfaces freeze.

Acceptance tests:
- Every task update maps to expected event.
- Steering affects next prompt context.
- Pause/resume/cancel are context-aware.
- Writes outside expected files are rejected through safety policy.

Documentation obligations:
- Update `docs/architecture.md` executor flow.
- Update `docs/contracts.md` executor event mapping.

Spec obligations:
- No task executor may bypass path or tool policy.

### 5.9 Detour and Deblocker System

Owner subagent role:
- Detour Worker.

Owned files:
- `internal/detour/*`
- Detour tests and fixtures
- API route implementation for detour only if coordinated with control-plane owner

Inputs:
- TaskSpec contract.
- State task/blocker/plan edit repositories.
- Provider router.
- Develop stage integration.

Outputs:
- Detour request capture.
- Minimal task generation.
- Detour task validation.
- Splice operation.
- Max-depth enforcement.
- Blocker creation on depth exceeded.

Dependencies:
- M1 contracts.
- M3 state.
- M4 provider fake.
- M8 executor integration.

Parallelism:
- Medium. Contracts/splice/provider flow can split after `internal/detour/contracts.go` exists.

Acceptance tests:
- Blocker triggers detour request.
- New tasks splice immediately after trigger task.
- ID conflicts detected.
- Depth exceeded pauses and creates blocker.

Documentation obligations:
- Update `docs/architecture.md` detour flow.
- Update `docs/contracts.md` detour schema.

Spec obligations:
- Never silently skip depth-exceeded detours.

### 5.10 Control Plane HTTP/SSE

Owner subagent role:
- Control Plane Worker.

Owned files:
- `internal/controlplane/server.go`
- `internal/controlplane/routes.go`
- `internal/controlplane/events.go`
- `internal/controlplane/sse.go`
- `internal/controlplane/errors.go`
- Control-plane tests
- Route-specific generated binding files if not generated elsewhere

Inputs:
- `api/openapi.yaml`
- Generated API types
- State event repository
- Auth middleware
- App service interfaces

Outputs:
- HTTP server with stdlib ServeMux.
- Route handlers.
- JSON error responses.
- Persisted SSE replay.
- Heartbeat.
- Bounded per-client queues.
- Slow-client handling.

Dependencies:
- M1 OpenAPI skeleton.
- M3 event store.
- M2 config.
- M11 auth for mutating routes.

Parallelism:
- Medium. SSE/event streaming can run separately from REST handlers after shared route/service contracts freeze.

Acceptance tests:
- `/health` works.
- All routes compile against generated types.
- JSON errors use `ErrorResponse`.
- SSE replay with `Last-Event-ID` works.
- Slow client is bounded and closed.

Documentation obligations:
- Update `docs/contracts.md` control-plane section.
- Later create/update `docs/API.md` once routes are implemented.

Spec obligations:
- Persist event before broadcast.
- No `[DONE]` SSE sentinel.

### 5.11 Auth and Permissions

Owner subagent role:
- Auth/Security Worker.

Owned files:
- `internal/controlplane/auth.go`
- `internal/safety/policy.go`
- `internal/safety/redaction.go`
- `internal/safety/prompt_injection.go`
- Security tests and fixtures
- `docs/SECURITY.md` when created

Inputs:
- Spec sections 13 and 16
- State auth token repository
- OpenAPI role metadata

Outputs:
- Opaque token creation/list/revoke.
- Token hashing and constant-time comparison.
- Role middleware.
- Remote-bind auth enforcement.
- CORS profile checks.
- Redaction and prompt injection detection.
- Tool policy parser/evaluator.

Dependencies:
- M2 config.
- M3 state auth tables.
- M10 control-plane routing skeleton.

Parallelism:
- Medium. Redaction/path/tool policy/auth can split if shared security contracts are defined.

Acceptance tests:
- Auth role matrix.
- Expired/revoked tokens fail.
- Remote bind without auth fails startup.
- CORS wildcard forbidden outside dev.
- Secret patterns scrubbed.
- Prompt injection fixtures produce warnings but cannot alter policy.

Documentation obligations:
- Create/update `docs/SECURITY.md`.
- Update `docs/contracts.md` auth roles.

Spec obligations:
- Do not implement homegrown stateless JWT as default.

### 5.12 MCP Tool Surface

Owner subagent role:
- MCP Worker.

Owned files:
- `internal/controlplane/mcp.go`
- `api/mcp_tools.json`
- MCP tests and fixtures

Inputs:
- OpenAPI route schemas.
- Auth role service.
- Control-plane service interfaces.

Outputs:
- Required MCP tools.
- Static tool descriptions.
- Input schemas matching OpenAPI where possible.
- Role enforcement identical to HTTP.
- MCP poisoning tests.

Dependencies:
- M1 OpenAPI.
- M10 control-plane service interfaces.
- M11 auth.

Parallelism:
- Medium after service interfaces freeze.

Acceptance tests:
- Tool list includes all required tools.
- MCP calls enforce observer/operator/admin roles.
- Tool descriptions cannot expand permissions.
- No arbitrary shell execution.

Documentation obligations:
- Update `docs/contracts.md` MCP section.
- Later update `docs/API.md`.

Spec obligations:
- MCP is a thin wrapper, not a second control plane.

### 5.13 CLI

Owner subagent role:
- CLI Worker.

Owned files:
- `cmd/nexdev/*`
- `internal/cli/*` if adopted
- CLI tests
- `README.md` quickstart sections when created

Inputs:
- Cobra dependency from geoffrussy or bootstrap.
- App service interfaces.
- Control-plane remote client.
- Config loader.

Outputs:
- Required command surface.
- Global flags.
- Local in-process mode.
- Remote `--control-url` mode.
- JSON output for CI.
- Auth token commands.

Dependencies:
- M2 config.
- M10/M11 control-plane/auth for remote and token commands.
- M5 pipeline services for local run/develop/verify.

Parallelism:
- Medium. Command groups can split after root command and shared services exist.

Acceptance tests:
- CLI smoke tests.
- Help output includes required commands.
- `config validate`, `doctor`, `status --json`, token create/list/revoke work when dependencies exist.

Documentation obligations:
- Update `README.md` and config/API docs for user-facing commands.

Spec obligations:
- `nexdev init --import-devussy PATH` must be included despite missing from initial CLI list.

### 5.14 TUI / Control UX

Owner subagent role:
- TUI Worker.

Owned files:
- `internal/tui/*`
- TUI tests where feasible

Inputs:
- Bubble Tea/Lipgloss stack from geoffrussy.
- State/event APIs.
- Control-plane client or in-process services.

Outputs:
- TUI views from spec.
- Keybindings.
- Plan editor.
- Detour dialog.
- Config/artifact/provider views.

Dependencies:
- M10 control-plane or in-process service interfaces.
- M7 planning/review.
- M9 detour.

Parallelism:
- Medium. Views can split after app model contracts freeze.

Acceptance tests:
- Model/update tests for keybindings.
- Plan edit events created through same service path.
- Quitting TUI does not kill run unless explicitly confirmed.

Documentation obligations:
- Update README and operations docs when TUI behavior exists.

Spec obligations:
- TUI is a client and must not own pipeline state.

### 5.15 Git Integration

Status: M2 project lock helper verified on 2026-06-30. Stage/task commit helpers, changed-file manifests, and optional worker worktree strategy remain later Git work.

Evidence for M2-PROJECT-LOCK:
- `internal/git/project_lock.go` defines `.nexdev/run/project.lock` path resolution through `internal/safety.PathSanitizer`.
- `AcquireProjectLock` creates the runtime parent directory, atomically creates the lock file with `O_EXCL`, writes pid and UTC timestamp metadata, rejects a second acquisition while held, and removes the lock on release.
- Stale lock detection is intentionally deferred; existing lock files are treated as held until M15 hardening defines recovery behavior.
- Evidence commands: `go test ./internal/git` passed; `go test ./...` passed.

Next action guidance:
- M3/M5/M8/M10 app, pipeline, executor, and control-plane workers should acquire this helper before mutating project-local state and release it during lifecycle shutdown.
- M15 security hardening should add stale-lock/process-liveness policy, race-hardening review around symlink swaps, and broader multi-process/race coverage.
- Spec section 21 worktree workers remain unimplemented; later Git work should keep default v0.1 execution on the single worktree unless explicitly assigned.

Owner subagent role:
- Git Worker.

Owned files:
- `internal/git/*`
- Git tests and fixtures

Inputs:
- Geoffrussy git package.
- Config commit policy.
- Changed-file artifact contract.

Outputs:
- Stage/task commit helpers.
- Changed-file manifest generation.
- Optional worker worktree scaffolding.
- Conflict blocker contract for worktree merges.

Dependencies:
- M2 config.
- M1 artifact contract.
- M8 executor for task ownership.

Parallelism:
- Low to medium. Changed-file manifest and commit metadata can split.

Acceptance tests:
- Commit metadata format.
- Changed-file manifest stable ordering and hashes.
- Worktree conflict creates blocker if optional worktree strategy implemented.

Documentation obligations:
- Update `docs/architecture.md` git flow.

Spec obligations:
- Default v0.1 strategy remains `single_worktree`.

### 5.16 Observability / Logging / Cost

Status: M2 logging baseline verified by Observability Worker on 2026-06-30. Full OpenTelemetry, runtime instrumentation, audit logs, and cost ledger remain M14 work.

Evidence:
- `internal/observability/logger.go` provides standard `log/slog` construction with caller-selected level and JSON/text modes.
- A redacting handler applies `internal/safety.RedactSecrets` to messages, string attrs, grouped attrs, and attrs attached through `Logger.With` before the underlying handler writes.
- Canonical helper attrs exist for `project_id`, `run_id`, `stage`, `task_id`, `provider`, `model`, `event_id`, and `request_id`.
- OpenTelemetry is intentionally disabled in M2 through a documented false placeholder; no OTel SDK/exporter, runtime instrumentation, metrics, audit logging, or cost ledger behavior was added.
- Evidence command: `go test ./internal/observability` passed.

Next action guidance:
- App/CLI wiring should construct loggers through `internal/observability.NewLogger` once config flag plumbing owns process startup.
- M10/M14 workers should add request/event correlation fields at control-plane/event boundaries without duplicating field-name constants.
- M14 should implement OTel, metrics, audit logs, and cost ledger only after M3 state and M4 provider usage metadata are available, keeping OTel disabled by default and adding no-secret-leak integration coverage.

Owner subagent role:
- Observability Worker.

Owned files:
- `internal/observability/*`
- Observability tests

Inputs:
- Config.
- Event contracts.
- Provider usage metadata.

Outputs:
- `slog` setup.
- Redacting handler integration.
- OTel optional setup.
- Metrics/tracing helpers.
- Cost ledger service.

Dependencies:
- M2 config.
- M4 provider usage metadata.
- M3 state if cost ledger persists.

Parallelism:
- Medium. Logging and OTel/cost can split after field names freeze.

Acceptance tests:
- Logs include required fields.
- Redaction applied.
- OTel disabled by default.
- Provider usage/cost records persisted or emitted as configured.

Documentation obligations:
- Update `docs/architecture.md` observability section.

Spec obligations:
- Logs/events/artifacts/prompts must scrub secrets.

### 5.17 Testing Infrastructure

Owner subagent role:
- Test Infrastructure Worker.

Owned files:
- `internal/testutil/*`
- `testdata/*` only if shared at root by decision
- `scripts/smoke.sh`
- `scripts/e2e_fake_provider.sh`
- CI workflow files in coordination with Foundation Worker
- `TESTING_STRATEGY.md`

Inputs:
- Testing Scout report.
- Contract fixtures.

Outputs:
- Shared black-box test helpers.
- Fake provider/worker fixtures where not owned by provider/executor.
- E2E fake-provider script.
- CI gates.

Dependencies:
- M0 bootstrap.
- M1 contracts.

Parallelism:
- Medium. Testutil, scripts, and CI can split after ownership is clear.

Acceptance tests:
- Test helpers are used only by tests.
- E2E fake-provider script passes when implementation reaches M16.
- CI runs required gates.

Documentation obligations:
- Keep `TESTING_STRATEGY.md` current.

Spec obligations:
- Real-provider tests must remain opt-in and spend-capped.

### 5.18 Documentation

Owner subagent role:
- Documentation Worker.

Owned files:
- `README.md`
- `docs/architecture.md`
- `docs/contracts.md`
- `docs/API.md`
- `docs/SECURITY.md`
- `docs/configuration.md`
- `docs/OPERATING.md`
- Documentation tests if added

Inputs:
- Worker handoffs.
- Implemented behavior.
- Spec.

Outputs:
- Current docs that match behavior.
- Quickstart.
- Architecture and contracts references.
- Security and operations guides.

Dependencies:
- Continuous; starts in planning and updates every milestone.

Parallelism:
- Medium, but one docs worker should reconcile style and contradictions before milestone acceptance.

Acceptance tests:
- Docs link checks if adopted.
- Docs reviewed by spec-management after each milestone.

Spec obligations:
- Secondary docs must not contradict `SPEC.md`.

### 5.19 Spec Management

Owner subagent role:
- Spec Management Subagent.

Owned files:
- `SPEC.md`
- `SPEC_UPDATE_PROTOCOL.md`
- Spec coverage matrix section in this file or later `docs/spec-coverage.md`

Inputs:
- Milestone handoffs.
- Code/tests/docs diffs.
- Orchestrator approvals.

Outputs:
- Spec updates for accepted implemented behavior, clarifications, deviations, and deferrals.
- Spec coverage matrix updates.
- Drift report.

Dependencies:
- Runs after each major milestone.

Parallelism:
- Low. One spec-management worker at a time.

Acceptance tests:
- Every completed requirement has implementation evidence and tests/docs references.
- No spec requirement weakened to hide incomplete work.

Documentation obligations:
- Update `SPEC_UPDATE_PROTOCOL.md` changelog.

Spec obligations:
- Source of truth stewardship.

### 5.20 Release / CI / Scripts

Owner subagent role:
- Release Worker.

Owned files:
- `.github/workflows/*`
- `scripts/*`
- Release config files
- `docs/OPERATING.md`

Inputs:
- Testing strategy.
- Build commands.
- Security requirements.

Outputs:
- CI gates.
- Smoke scripts.
- Release scripts.
- Optional SBOM/checksum/signing flow.

Dependencies:
- M0 bootstrap.
- M16 E2E for final smoke.

Parallelism:
- Medium.

Acceptance tests:
- CI command parity with documented commands.
- `go test`, `go vet`, `govulncheck`, fake-provider E2E, and smoke scripts pass by release readiness.

Documentation obligations:
- Update operating/release docs.

Spec obligations:
- Supply-chain checks required before v0.1 complete.

## 6. Contracts-First Build Order

Create or freeze these before broad feature development:

1. Repository bootstrap decision record.
2. Module path and Go version.
3. OpenAPI skeleton: `api/openapi.yaml`.
4. `ErrorResponse` schema.
5. Event envelope and event type constants.
6. Stage names, order, statuses, and transition rules.
7. State migration plan and durable table names.
8. Artifact kind enum and artifact manifest schema.
9. Structured model output schemas.
10. `TaskSpec`, plan DAG validation contract, task statuses.
11. Provider interface/router slots and structured output wrapper contract.
12. Event publisher interface.
13. State repository interfaces.
14. Pipeline `PipelineStage` and `StageEnv` interfaces using small interfaces to avoid import cycles.
15. Executor control/update interfaces and event mapping.
16. Steering message/source/context contracts.
17. Detour request/result/splice/depth contracts.
18. Auth role matrix and token storage schema.
19. Tool policy and path safety contracts.
20. Test fixtures for fake provider, fake worker, fake IDs/time, security repos, and SSE replay.

First-wave contract tasks:

| Task ID | Goal | Likely Files | Owner | Depends On | Acceptance | Tests | Docs | Spec Impact |
|---|---|---|---|---|---|---|---|---|
| C1 | Create OpenAPI skeleton with every required route and common schemas | `api/openapi.yaml` | Contract/API Worker | M0 | OpenAPI validates and route list matches spec | OpenAPI validation | `docs/contracts.md` | None unless routes change |
| C2 | Define event envelope and event type constants | `internal/contract/events.go` | Contract/API Worker | C1 or M0 | Event version and all required event names exist | Event golden tests | `docs/contracts.md` | None |
| C3 | Define stage/task/status contracts | `internal/contract/pipeline.go`, `internal/pipeline/stage.go` | Pipeline Framework Worker | M0 | Canonical order and transitions match spec | Stage graph tests | `docs/contracts.md` | None |
| C4 | Define model output and artifact contracts | `internal/contract/model_outputs.go`, `internal/contract/artifacts.go` | Contract/API Worker | M0 | Structs/schemas cover required artifacts | Schema/golden tests | `docs/contracts.md` | None |
| C5 | Define state migrations skeleton | `internal/state/migrations/*` | State Worker | C2-C4 | Required tables/indexes present | Migration tests | `docs/contracts.md` | None |
| C6 | Define provider/router contracts | `internal/provider/provider.go`, `internal/provider/router.go` | Provider Worker | C4 | Slots match spec and inherit primary | Router tests | `docs/architecture.md` | None |
| C7 | Define executor/steering/detour interfaces | `internal/executor/contracts.go`, `internal/steering/contracts.go`, `internal/detour/contracts.go` | Executor/Detour Workers | C2-C4 | Interfaces compile without import cycles | Contract compile tests | `docs/contracts.md` | None |
| C8 | Define auth role metadata | `api/openapi.yaml`, `internal/controlplane/auth.go` skeleton | Auth Worker | C1, C5 | Role matrix mapped to routes | Auth matrix skeleton tests | `docs/contracts.md` | None |
| C9 | Define shared test fixtures | `internal/testutil/*`, `testdata/*` | Test Infrastructure Worker | C2-C5 | Fakes support deterministic IDs/time/provider/events | Fixture tests | `TESTING_STRATEGY.md` | None |

First-wave progress:
- M1-C1/C2/C4: verified by Contract/API Worker on 2026-06-29. Added `api/openapi.yaml` with all required `SPEC.md` section 12.2 routes, common first-wave schemas, and `x-nexdev-role` metadata; added inert event, artifact, changed-file, run-summary, structured model output, detour, and verification contracts in `internal/contract`. Evidence: `go test ./internal/contract` passed; `go test ./...` passed.
- M1-C1/C2/C4 next-action guidance: Keep `api/openapi.yaml` authoritative until generated code is introduced. Generated OpenAPI types remain deferred to M1 integration/spec-management or the owning API worker after the codegen path is settled; downstream workers should not hand-roll divergent route or schema contracts.
- M1-C3: verified by Pipeline Framework Worker on 2026-06-29. Added `internal/pipeline` contracts for canonical stage names/order, detour pseudo-stage, stage statuses, allowed status transitions, prerequisite requirement keys/snapshot validation, and minimal import-cycle-safe `PipelineStage`/`StageEnv` interfaces. Evidence: `go test ./internal/pipeline` passed; `go test ./...` passed.
- M1-C3 next-action guidance: State, provider, executor, detour, and control-plane workers should import `internal/pipeline` stage/status identifiers rather than duplicating string constants. M5 may extend the same package with durable runner/resumption after M3 state and M4 provider contracts are available.
- M1-C5: verified by State Worker on 2026-06-30. Added Nexdev state migration skeleton as additive migration version `4` in the imported geoffrussy migration runner, covering `runs`, `stage_runs`, `events`, `artifacts`, `hivemind_results`, `validate_results`, `steering_events`, `detour_records`, `navigation_events`, `plan_edit_events`, and `auth_tokens`. Added state tests for required table/index existence, seeded geoffrussy-compatible migration, event sequence uniqueness, and busy timeout. Evidence: `go test ./internal/state` passed; `go test ./...` passed.
- M1-C5 next-action guidance: M3 State Workers should keep version `4` as the contract baseline, add repositories without replacing the migration runner unless orchestrated, and implement event sequence allocation/concurrency tests before SSE/control-plane work depends on the event log. `auth_tokens` is schema-only until the M3/M10 auth repository and middleware tasks.
- M1-C6: verified by Provider Worker on 2026-06-30. Added Nexdev provider slot constants and `internal/provider.Router` contract for resolving stage slots to provider/model selections while preserving the imported geoffrussy `Provider` interface and registry. Empty slot selections inherit primary; unknown slots/providers are rejected. Evidence: `go test ./internal/provider` passed; `go test ./...` passed.
- M1-C6 next-action guidance: M4 Provider Worker should build structured-output and fake-provider behavior on top of `Router.Resolve` instead of changing the imported `Provider` interface first. Provider Worker C6 reported `clarification-needed` because the imported geoffrussy `Provider` interface differs materially from `SPEC.md` section 11.1. Keep that mismatch visible for M1 spec-management/M4 provider design; do not hide it by treating imported behavior as a replacement for the spec.

- M1-C7: verified by Executor/Detour Worker on 2026-06-30. Added compile-safe executor control/report/update-event contracts in `internal/executor/contracts.go`, steering source/message/context contracts in `internal/steering/contracts.go`, and detour request/context/generator/splice/depth contracts in `internal/detour/contracts.go`. Evidence: `go test ./internal/executor ./internal/detour ./internal/steering` passed; `go test ./...` passed.
- M1-C7 next-action guidance: M8 should implement executor controls and the event bridge against `TaskUpdateEventMapping` without changing event names. M8 steering work should persist `steering.Message` through M3 state repositories and preserve `SafetyPolicyOverrideAllowed=false`. M9 should implement detour generation/splicing behind `detour.Generator`, `detour.Splicer`, and `detour.DepthPolicy`, using `contract.TaskSpec` and blocker reason `detour_depth_exceeded` for depth exhaustion.
- M1-C8: verified by Auth/Security Worker on 2026-06-30. Added compile-safe auth role constants, role hierarchy checks, route role metadata mirroring OpenAPI `x-nexdev-role`, MCP `per-tool` delegation, token record contract shape, and remote-bind auth requirement helper in `internal/controlplane/auth.go`. Evidence: `go test ./internal/controlplane` passed; `go test ./...` passed.
- M1-C8 next-action guidance: M10 should wire HTTP middleware and startup/config auth enforcement through `RouteRoles`, `AllowsRoute`, and `RequireAuthForBind` rather than duplicating route role strings. M11 should resolve MCP tool-specific roles before calling `AllowsRoute` for `POST /mcp/call`; tool descriptions must not expand permissions. M3/M10 auth repository work should implement opaque token hashing, expiry, revocation, last-used updates, and constant-time comparisons against the existing `auth_tokens` schema without changing this contract unless orchestrated.
- M1-C9: verified by Test Infrastructure Worker on 2026-06-30. Added `internal/testutil` black-box fixture contracts for deterministic UTC time, stable fixture IDs, minimal temp projects with safe defaults, event recording/sequence assertions over `internal/contract.EventEnvelope`, and auth role fixtures using `internal/controlplane` role constants without token storage behavior. Evidence: `go test ./internal/testutil` passed; `go test ./...` passed.
- M1-C9 next-action guidance: M16/fake-provider workers should extend `internal/testutil` rather than creating duplicate ID/time/temp-project fixtures. Add fake provider, fake worker, SSE client/replay helpers, golden-path helpers, and security fixture repos only when their owning feature tests exist; keep provider/worker behavior deterministic and test-only. If docs-contract coverage for test fixtures is desired, assign a contracts/docs follow-up rather than editing `docs/contracts.md` ad hoc.

## 7. Milestone DAG

This is a DAG, not a flat checklist. A milestone may start only when all prerequisite milestones and listed contract gates are satisfied. Within a milestone, subagents can run in parallel only when file ownership is non-overlapping.

Status values for later session:
- `planned`
- `in_progress`
- `implemented`
- `verified`
- `blocked`
- `deferred`

### M0. Repository Assessment and Project Bootstrap

Status: verified by Foundation Worker on 2026-06-29.

Evidence:
- Geoffrussy import source: `https://github.com/mojomast/geoffrussy.git` at `e29f8e7649584585a93d8fc8ac9123036fcaf38e`.
- Module path: `github.com/mojomast/nexdev`.
- Preserved planning artifacts: `SPEC.md`, `DEVPLAN.md`, `AGENTS.md`, `WORKER_PROTOCOL.md`, `SPEC_UPDATE_PROTOCOL.md`, `TESTING_STRATEGY.md`, `PROMPT_FOR_DEVELOPMENT_SESSION.md`, `docs/architecture.md`, `docs/contracts.md`, and `README.md`.
- Baseline commands: `go test ./...` passed; `go vet ./...` passed.
- Upstream compatibility note: temporary geoffrussy assessment failed `go test ./...` before import due a missing `internal/design.extractJSONFromMarkdown` helper referenced by tests; M0 added the helper after import without removing tests.

Historical next action guidance:
- Completed: M1 contract freeze started only after M0 verification and preserved-doc checks.
- Completed: first-wave work prioritized `api/openapi.yaml`, event envelope/constants, stage/status contracts, state migration skeleton, provider/router contracts, auth route metadata, executor/steering/detour interfaces, and shared test fixtures before feature-stage work.
- Still relevant: run M1 spec-management before broad M2-M5 implementation to classify the Provider C6 interface mismatch and the generated OpenAPI codegen deferral.

Purpose:
- Convert the planning-only repo into a buildable Go project, preferably by forking/importing geoffrussy while preserving the canonical spec and planning docs.

Prerequisites:
- None.

Subagents that can run in parallel:
- Foundation Worker performs bootstrap.
- Documentation Worker verifies planning docs are preserved.
- Test Infrastructure Worker prepares test command plan without editing production code until module exists.

Contracts required before starting:
- None beyond `SPEC.md` and this plan.

Expected files touched:
- `go.mod`
- `go.sum`
- `.gitignore`
- `.github/workflows/*` initial optional
- `cmd/nexdev/main.go` minimal only if needed for compile
- Existing planning docs must be preserved.

Tests required:
- `go test ./...` if module exists.
- `go vet ./...` if compileable.

Docs updated:
- `DEVPLAN.md` milestone status.
- `docs/architecture.md` base-repo decision.

Spec update required:
- Required only if not using geoffrussy fork/import.

Merge/integration checkpoint:
- Orchestrator verifies `SPEC.md` and planning docs are intact before any feature work.

Acceptance criteria:
- Buildable Go module exists.
- Base strategy recorded.
- No production feature implementation beyond bootstrap skeleton.
- Initial tests compile or blockers documented.

### M1. Contract Freeze: Schemas, Interfaces, Event Envelopes, Ownership Rules

Status: first-wave verified. C1-C9 contract workers completed blocker-free and the orchestrator verification passed `go test ./...`, `go vet ./...`, and `go mod verify`. M1 remains open only for integration/spec-management reconciliation, the generated OpenAPI codegen decision, and the Provider C6 `clarification-needed` follow-up for the imported geoffrussy `Provider` interface mismatch with `SPEC.md` section 11.1.

Evidence for C1/C2/C4:
- `api/openapi.yaml` exists with all required `SPEC.md` section 12.2 routes, common first-wave schemas, and `x-nexdev-role` operation metadata.
- `internal/contract/events.go` defines event contract version `nexdev-event-v1`, required event type constants, event sources, and `EventEnvelope`.
- `internal/contract/artifacts.go` and `internal/contract/model_outputs.go` define inert artifact, changed-file, run-summary, structured model output, detour, and verification structs.
- `internal/contract/openapi_test.go` and `internal/contract/events_test.go` validate route/role coverage, common schema presence, event constants, and event sources.

Evidence for C7:
- `internal/executor/contracts.go` defines context-aware executor control interfaces, current-task snapshots, task report shape, task status constants, and `TaskUpdate` to Nexdev task event mapping.
- `internal/steering/contracts.go` defines steering source/message/context/store contracts and pins safety override prohibition.
- `internal/detour/contracts.go` defines shared detour request/result aliases, request context, generator/splicer/depth interfaces, and default depth contract.
- `internal/executor/contracts_test.go`, `internal/steering/contracts_test.go`, and `internal/detour/contracts_test.go` validate interface compilation and mapping coherence.

Evidence for C3/C5/C6/C8/C9:
- `internal/pipeline` owns canonical stage/status/prerequisite contracts; `go test ./internal/pipeline` passed.
- `internal/state` migration version `4` adds the Nexdev contract tables and indexes while preserving the imported geoffrussy migration runner; `go test ./internal/state` passed.
- `internal/provider.Router` resolves stage provider slots, validates registry names, and inherits primary provider/model selections; `go test ./internal/provider` passed. The imported provider interface mismatch remains a spec-management/M4 risk.
- `internal/controlplane/auth.go` defines role hierarchy, route metadata, token record shape, MCP `per-tool` delegation, and non-loopback auth helper; `go test ./internal/controlplane` passed.
- `internal/testutil` defines black-box fixture contracts for temp projects, deterministic time/IDs, event recording, and auth role fixtures; `go test ./internal/testutil` passed.

Next action guidance:
- Run M1 spec-management before starting broad M2-M5 implementation. It should classify the C6 provider interface mismatch, confirm that generated OpenAPI codegen remains deferred or assign the codegen worker, and record any accepted clarifications without weakening `SPEC.md`.
- M3 State Workers should build repositories on the version `4` migration skeleton, preserve geoffrussy compatibility, and complete event sequence allocation/concurrency before M10 SSE/control-plane work begins.
- M4 Provider Worker should keep using `Router.Resolve` for provider slot selection while spec-management decides whether to refactor toward the `SPEC.md` section 11.1 interface, document a compatibility wrapper, or approve a contract correction.
- M5 Pipeline workers should import `internal/pipeline` stage/status identifiers and avoid duplicating string constants.
- M8/M9 workers should implement executor, steering, and detour behavior behind the C7 interfaces without changing event names or safety override rules.
- M10 Control Plane Worker should wait for M1 integration/spec-management and the codegen-location decision before binding handlers to `api/openapi.yaml`.
- Generated OpenAPI types remain deferred until the codegen tool path is settled; dependent workers should treat `api/openapi.yaml` as the machine contract.
- M10 control-plane/auth workers should import `internal/controlplane` auth helpers and keep `POST /mcp/call` role resolution delegated to M11 per-tool metadata.
- M16 fake-provider/E2E work should extend `internal/testutil`; no fake-provider E2E script exists yet.

Purpose:
- Create stable contracts that unblock parallel workers.

Prerequisites:
- M0 verified.

Subagents that can run in parallel:
- Contract/API Worker: OpenAPI and error schemas.
- State Worker: migration skeleton after table contract review.
- Pipeline Framework Worker: stage/status contracts.
- Provider Worker: provider/router contracts.
- Executor/Detour Worker: executor, steering, detour interfaces.
- Test Infrastructure Worker: fixture contracts.

Contracts required before starting:
- Module path.
- Generated-code location decision.

Expected files touched:
- `api/openapi.yaml`
- `internal/contract/*`
- `internal/state/models.go`
- `internal/state/migrations/*`
- `internal/pipeline/stage.go`
- `internal/provider/provider.go`
- `internal/provider/router.go`
- `internal/executor/contracts.go`
- `internal/detour/contracts.go`
- `internal/steering/contracts.go` if adopted
- `internal/testutil/*`
- `docs/contracts.md`

Tests required:
- Contract compile tests.
- OpenAPI validation.
- Event golden tests.
- Stage graph contract tests.
- Migration schema existence tests.

Docs updated:
- `docs/contracts.md`.
- `docs/architecture.md` package ownership.
- `TESTING_STRATEGY.md` contract test section.

Spec update required:
- After milestone, spec-management reviews any resolved ambiguities.

Merge/integration checkpoint:
- Orchestrator runs contract tests and checks no feature worker starts against unstable files.

Acceptance criteria:
- First-wave contracts compile.
- File ownership boundaries are documented.
- No open contract blocker remains for M2-M5.

### M2. Config, Paths, Logging, Security Baseline

Status: verified for M2 helper baselines. M2-CONFIG-PATHS, M2-SECURITY-BASELINE, M2-OBSERVABILITY-LOGGER, and M2-PROJECT-LOCK are verified; app/executor/control-plane integration remains later milestone work.

Evidence for M2-CONFIG-PATHS:
- `internal/config/nexdev.go` and tests implement typed defaults, profile validation, `auth_required: auto` resolution, remote bind rejection when auth is explicitly disabled, and unknown top-level key rejection with `experimental.allow_unknown_config` override.
- `internal/safety/paths.go` and tests implement path traversal, absolute outside-root, `.git`, symlink escape, and deny-glob rejection.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/config ./internal/safety` passed.

Evidence for M2-SECURITY-BASELINE:
- `internal/safety/redaction.go` implements deterministic best-effort redaction for provider/API key shapes, bearer tokens, password/token assignments, private key blocks, SSH keys, and `.env` style secret assignments.
- `internal/safety/prompt_injection.go` implements warning-only prompt-injection findings for common instruction override, prompt exfiltration, role override, safety bypass, and secret exfiltration strings.
- `internal/safety/policy.go` implements a non-executing tool policy skeleton with default-deny shell/network, read/write basics, deny globs for `.git`, `.env`, private keys, PEM/key files, and wildcard shell allow-rule rejection in `trusted-lan` and `ci` profiles.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/safety` passed; `go test ./...` passed.

Evidence for M2-OBSERVABILITY-LOGGER:
- `internal/observability/logger.go` implements the spec-target slog baseline with level selection, JSON/text modes, and a redacting handler that applies `internal/safety.RedactSecrets` to messages and string attributes before write.
- Required log field helpers exist for `project_id`, `run_id`, `stage`, `task_id`, `provider`, `model`, `event_id`, and `request_id`.
- OpenTelemetry, metrics, audit logs, runtime instrumentation, and cost ledger behavior remain unimplemented and deferred to M14.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/observability` passed; `go test ./...` passed.

Evidence for M2-PROJECT-LOCK:
- `internal/git/project_lock.go` implements `.nexdev/run/project.lock` path resolution, exclusive acquire, pid/timestamp metadata, held-lock detection, and release removal.
- `internal/git/project_lock_test.go` covers lock path, acquire/release metadata, second-acquire failure, release/reacquire, and symlink escape rejection where symlinks are available.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/git` passed; `go test ./...` passed.

Next action guidance:
- M3/M5/M8/M10 workers should wire `internal/git.AcquireProjectLock` into mutating lifecycle paths before control-plane/executor integration depends on project-local runtime behavior.
- M15 hardening should define stale-lock cleanup/process-liveness policy and add broader multi-process/race coverage for project mutation exclusion.
- M8/M15 executor/verify/security workers must wire `ToolPolicy` into command and network-capable tool paths before any execution exists, load `.nexdev/tool_policy.yaml` only through an owned policy-loader task, and keep wildcard shell allow rules invalid outside `dev`.
- M6/M7/M10/M14 workers should route untrusted repo/tool text through `DetectPromptInjection` and emit/surface `security_warning` findings where their domains own events or review output.
- M10/M14 observability/control-plane/provider workers should construct loggers through `internal/observability`, attach canonical request/event/provider/stage/task fields, apply `RedactSecrets` at event/artifact/prompt/API boundaries, and add integration tests for no secret leakage.
- M14 should add OTel, metrics, audit logs, and cost ledger behavior only after M3 state and M4 provider usage metadata are available; OTel must remain disabled by default.
- Defer full config precedence wiring to CLI/app integration unless the orchestrator assigns a config-loader task with ownership of root command/app wiring.
- Keep `SPEC.md` unchanged; `auth_required: auto` behavior is documented as implementation detail pending spec-management review.

Purpose:
- Establish safe runtime defaults and common security/logging foundations.

Prerequisites:
- M1 verified.

Subagents that can run in parallel:
- Config/Safety Worker: config loader and validation.
- Auth/Security Worker: redaction, prompt-injection detection, tool policy skeleton.
- Observability Worker: `slog` setup.
- Git Worker: project lock path helper if separated.

Contracts required before starting:
- Config schema.
- Path policy contract.
- Log field/redaction contract.

Expected files touched:
- `internal/config/*`
- `internal/safety/*`
- `internal/observability/logger.go`
- `docs/architecture.md`
- `docs/contracts.md`
- `TESTING_STRATEGY.md`

Tests required:
- Config precedence and unknown-key tests.
- Profile validation tests.
- Path traversal/symlink tests.
- Project lock path/acquire/release tests.
- Redaction tests.
- Prompt-injection warning tests.

Docs updated:
- Configuration section in architecture/contracts.
- Security baseline notes.

Spec update required:
- Resolve `auth_required: auto` and commit config precedence ambiguity if implementation fixes it.

Merge/integration checkpoint:
- Verify secure defaults before control-plane or executor work depends on them.

Acceptance criteria:
- Remote bind without auth is rejected by config validation or startup checks.
- Command execution default is deny.
- Safety helpers are ready for state/control/executor integration.

### M3. SQLite State, Migrations, Repositories, Fixtures

Status: verified for assigned M3 repository groups. State Worker B verified the M3 event repository on 2026-06-30; State Worker C verified run, stage-run, and artifact repositories on 2026-06-30; State Worker D verified auth token, steering, detour, navigation, and plan edit repositories on 2026-06-30. The M3 task/blocker contract follow-up was implemented on 2026-06-30 with additive migration version `5` for Nexdev-specific task/blocker persistence. Higher-level auth/control-plane, steering prompt context, detour manager, navigation runner, and review editing integrations remain later milestone work.

Evidence for M3-STATE-EVENTS:
- `internal/state/events.go` implements persisted `contract.EventEnvelope` storage on migration version `4`, transactional per-run sequence allocation, caller-sequence conflict rejection, UTC RFC3339Nano timestamp storage/load, event ID to sequence mapping, and replay queries after sequence or event ID.
- `internal/state/events_test.go` covers persist/load, monotonic sequence per run, independent sequences across runs, replay after sequence/event ID, unsafe sequence conflicts, duplicate event IDs, and concurrent publishers.
- Documentation updates are recorded in `docs/contracts.md` and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/state` passed.

Evidence for M3-STATE-RUNS-ARTIFACTS:
- `internal/state/runs.go` implements `Store` repositories for `runs` and `stage_runs` against migration version `4`, including create/get/update/list helpers, complete/cancel helpers, stage output/error JSON persistence, attempt updates, UTC RFC3339Nano timestamp normalization, and bounded retrying transactions for writes.
- `internal/state/artifacts.go` implements artifact upsert/get/list by project/run/kind, preserving metadata JSON and using the artifact `id` primary key for upsert because version `4` defines no additional artifact uniqueness constraint.
- `internal/state/runs_test.go` and `internal/state/artifacts_test.go` cover create/read/update/list behavior, FK behavior, JSON metadata/output/error round-trips, timestamp normalization, and stable ordering.
- Documentation updates are recorded in `docs/contracts.md` and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/state` passed.

Evidence for M3-STATE-AUX-REPOS:
- `internal/state/auth_tokens.go` implements repository-level auth token create/get/get-by-hash/list/revoke/touch behavior while storing only `token_hash` and metadata. Token random generation, hashing, constant-time comparison, expiry authorization, and role middleware remain M10 auth/security work.
- `internal/state/steering.go`, `detours.go`, `navigation.go`, and `plan_edits.go` implement append/create/list helpers over migration version `4`, preserve JSON fields where applicable, normalize persisted timestamps to UTC RFC3339Nano, and use bounded retrying transactions for writes.
- `internal/state/auth_tokens_test.go`, `steering_test.go`, `detours_test.go`, `navigation_test.go`, and `plan_edits_test.go` cover create/read/list/update/revoke where applicable, FK/unique behavior, JSON round-trips, timestamp normalization, and stable ordering.
- Documentation updates are recorded in `docs/contracts.md` and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/state` passed.

Evidence for M3-FOLLOWUP-TASK-BLOCKER-CONTRACT:
- `internal/state/migrations.go` adds additive migration version `5` with `nexdev_tasks` and `nexdev_blockers`, preserving legacy geoffrussy `tasks` and `blockers` tables.
- `internal/state/tasks.go` persists `contract.TaskSpec` fields plus project/run/status/plan version/order metadata, validates required fields and same-run dependency references, preserves JSON slice fields, and lists tasks in stable plan order.
- `internal/state/blockers_nexdev.go` persists Nexdev blocker/control-plane fields, validates metadata JSON, lists by run/task/status in stable order, and resolves blockers with UTC timestamps.
- `internal/state/tasks_test.go`, `blockers_nexdev_test.go`, and `migrations_test.go` cover task CRUD/list/status/dependency JSON behavior, blocker create/resolve/list behavior, FK/unique behavior, additive migration from existing DB, and legacy table compatibility.
- Documentation updates are recorded in `docs/contracts.md` and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/state` passed.

Next action guidance:
- M5 runner work should use the run/stage repositories for durable resumption and enforce status transitions/canonical stage validation outside `internal/state`.
- M7 pipeline/artifact work should use the artifact repository for indexing after writing files, should select stable artifact IDs/kinds, and should emit `artifact_updated` through the event path after persistence.
- M7 planning/review work should persist approved `contract.TaskSpec` rows through `Store.CreateNexdevTask` after validating cycles, expected-file requirements, plan version/order, and review mutations; it should write `plan_edit_events` through `Store.CreatePlanEditEvent` only after a validated plan mutation and version increment. Event emission remains outside `internal/state`.
- M8 executor work should load reviewed tasks through `Store.ListNexdevTasks`, update status through `Store.UpdateNexdevTaskStatus`, and create structured blockers through `Store.CreateNexdevBlocker` rather than relying on imported geoffrussy `tasks` or `blockers` semantics.
- M8 steering work should read `Store.ListSteeringEvents` for prompt context and implement last-N plus summary behavior without allowing safety-policy overrides.
- M9 detour work should use `Store.ListNexdevBlockers`, `CreateNexdevBlocker`, and `ResolveNexdevBlocker` for blocker flow, persist validated detour results through `Store.CreateDetourRecord`, create spliced tasks through `Store.CreateNexdevTask`, and coordinate resulting plan edits with the plan edit repository.
- M10 SSE/control-plane work should call `Store.PersistEvent` before broadcast and replay with `Store.ListEvents` using `AfterEventID` for `Last-Event-ID`.
- M10 should decide HTTP behavior for unknown `Last-Event-ID` values and enforce configured replay caps at the control-plane layer, not in the state repository.
- M10 auth work should use `Store.GetAuthTokenByHash`, `RevokeAuthToken`, `TouchAuthTokenLastUsed`, and `ListAuthTokens` while keeping token generation/hashing/comparison in auth middleware/CLI services.
- M11 MCP work should pass authenticated actors through the same service paths that create steering/navigation/plan edit records.

Purpose:
- Build durable SQLite state foundation with event log and required repositories.

Prerequisites:
- M1 verified.
- M2 config/path helpers available for state paths.

Subagents that can run in parallel:
- State Worker A: migration runner and DB setup.
- State Worker B: event repository.
- State Worker C: run/stage/artifact repositories.
- State Worker D: auth/steering/detour/plan-edit repositories.
- Test Infrastructure Worker: migration fixtures.

Contracts required before starting:
- Table names and schemas.
- Time/ID serialization helpers.
- sqlc/goose adoption decision.

Expected files touched:
- `internal/state/*`
- `internal/state/migrations/*`
- `internal/state/testdata/*`
- `internal/testutil/*`
- `docs/contracts.md`

Tests required:
- Empty DB migration.
- Seeded geoffrussy migration.
- Idempotency.
- Foreign key enforcement.
- WAL/busy timeout.
- Event sequence uniqueness and concurrency.

Docs updated:
- State schema and migration notes.

Spec update required:
- If geoffrussy state compatibility cannot be preserved, escalate blocker.

Merge/integration checkpoint:
- Orchestrator runs migration and event repository tests before M10 SSE/control-plane starts.

Acceptance criteria:
- SQLite is authoritative for run, event, task/blocker, steering, detour, auth, and artifact state contracts.
- Event log supports persisted-before-broadcast semantics.

M1-C5 baseline for M3:
- Migration version `4` already creates the required Nexdev contract tables and indexes additively on top of imported geoffrussy state.
- `Store.open` enables foreign keys, WAL, and `busy_timeout`; retry helpers remain imported geoffrussy behavior.
- M3 should add typed repositories, event sequence allocation, concurrency/contention coverage, and auth/steering/detour/plan-edit persistence without changing table names unless spec-management approves a contract update.

### M4. Provider Registry and Fake Provider Harness

Status: fake-provider harness verified for M4. M1-C6 router contracts are verified, M4-PROVIDER-STRUCTURED added the structured output compatibility wrapper on 2026-06-30, and M4-PROVIDER-FAKE added deterministic fake provider scripting on 2026-06-30. Raw-response persistence/audit hooks and usage/cost ledger integration remain planned follow-up work.

Evidence for M4-PROVIDER-STRUCTURED:
- `internal/provider/structured.go` defines `StructuredClient.CallStructured` over the imported `Provider.Call(ctx, model, prompt)` interface without refactoring concrete providers.
- The wrapper resolves provider/model through `Router.Resolve`, strict-decodes JSON with unknown-field rejection by default, runs optional semantic validation, repairs decode/validation failures up to the configured cap, redacts provider errors with `internal/safety.RedactSecrets`, and returns raw response text, attempts, provider/model metadata, validation errors, and response usage metadata.
- `internal/provider/structured_test.go` covers valid decode, unknown field rejection, repair success, repair cap failure, semantic validation failure, slot/model resolution, usage metadata capture, and provider error redaction.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/provider` passed.

Evidence for M4-PROVIDER-FAKE:
- `internal/provider/fake.go` defines constructor-only `FakeProvider` implementing the imported geoffrussy `Provider` interface without registering it globally.
- The fake provider scripts deterministic responses by model and prompt matcher, supports structured invalid-then-repair flows through `StructuredClient`, unrecoverable invalid responses, retryable and hard errors, usage metadata, deterministic latency call records without sleeps, streaming chunks, model list/discovery, optional auth-required behavior, and coding-plan capability metadata.
- `internal/provider/fake_test.go` covers basic call, structured repair through `StructuredClient`, unrecoverable invalid response, error scripting, usage metadata, streaming chunks, model listing/discovery, authentication behavior, and disabled-by-default registry behavior.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification command: `go test ./internal/provider` passed.

Next action guidance:
- M6 pipeline workers should construct `FakeProvider` explicitly for stage tests and call JSON-producing stages through `StructuredClient` rather than duplicating structured validation behavior.
- Pipeline workers should call `StructuredClient.CallStructured` for JSON-producing stages and use returned raw/validation metadata for later audit persistence instead of calling providers directly.
- State/observability follow-up should persist raw responses, validation errors, latency, and cost metadata from wrapper results/events when those integration seams are assigned.
- Spec-management should record the accepted compatibility-wrapper decision for the `SPEC.md` section 11.1 imported-interface mismatch without weakening the structured-output requirements.
- M16 E2E work should wire the fake provider by explicit constructor/config path; it must not rely on global provider registry registration.

Purpose:
- Preserve geoffrussy provider boundary and add structured/fake-provider support.

Prerequisites:
- M1 contracts.
- M2 config.
- M3 state hooks for usage/audit if needed.

Subagents that can run in parallel:
- Provider Worker A: registry/router slots.
- Provider Worker B: structured output wrapper.
- Provider Worker C: fake provider and fixtures.
- Observability Worker: usage/cost integration.

Contracts required before starting:
- Model output schemas.
- Provider slot names.
- Usage metadata shape.

Expected files touched:
- `internal/provider/*`
- `internal/provider/testdata/*`
- `internal/testutil/*`
- `docs/architecture.md`
- `TESTING_STRATEGY.md`

Tests required:
- Provider routing.
- Structured decode strictness.
- Repair attempts.
- Fake provider deterministic responses.
- Usage/cost metadata capture.

Docs updated:
- Provider architecture and fake-provider testing guide.

Spec update required:
- None unless provider interface differs from spec due geoffrussy reality.

Merge/integration checkpoint:
- Fake provider must be accepted before pipeline stage implementation begins.

Acceptance criteria:
- Stages can call only provider router/wrapper.
- CI can run without real provider keys.

### M5. Pipeline Runner and Stage Framework

Status: verified for M5 runner framework on 2026-06-30. Durable runner registration, prerequisite validation integration, status transition enforcement, checkpoint-like stage output/error persistence, persisted event emission, and resume selection from M3 run/stage state are implemented. Concrete product stages remain M6/M7 work.

Evidence:
- `internal/pipeline/runner.go` implements `Runner`, canonical stage registration, `Run`, `Resume`, `RunOptions.From`, `RunOptions.SingleStage`, `PrerequisiteProvider`, optional `StageOutputter`, skipped and blocked stage signals, status transition validation through `ValidateStatusTransition`, checkpoint output/error persistence through M3 `Store` methods, and persisted `stage_status`/`done` events through `Store.PersistEvent`.
- `internal/pipeline/runner_test.go` covers canonical fake-stage execution order, prerequisite rejection, persisted invalid status transition rejection, resume after a completed persisted stage, skipped checkpoint output, blocked/failed stage persistence, and event emission.
- Documentation updates are recorded in `docs/architecture.md`, `docs/contracts.md`, and `TESTING_STRATEGY.md`.
- Verification commands: `go test ./internal/pipeline` passed; `go test ./...` passed.

Next action guidance:
- M6 stage workers should register concrete stages with this runner rather than duplicating navigation/status logic, should provide concrete prerequisite facts through `PrerequisiteProvider`, and should call provider structured-output wrappers for model-backed JSON.
- M7 artifact/review workers should use stage output checkpoints only for runner state and must separately write/index artifact files through the artifact repository before emitting `artifact_updated`.
- M10 control-plane and M12 CLI workers should wire `RunOptions.SingleStage`, `RunOptions.From`, and `Resume` to public commands/APIs and should broadcast only events already persisted by the state event repository.
- App/CLI lifecycle work still needs to acquire `.nexdev/run/project.lock` around mutating runner invocations.

Purpose:
- Implement durable stage orchestration, prerequisite validation, checkpointing, and resumption.

Prerequisites:
- M1 contracts.
- M3 state.
- M4 provider interfaces for `StageEnv`.

Subagents that can run in parallel:
- Pipeline Framework Worker only for runner/navigation.
- Test Infrastructure Worker for runner fixtures.

Contracts required before starting:
- Stage interface.
- Event publisher interface.
- Run/stage repositories.

Expected files touched:
- `internal/pipeline/stage.go`
- `internal/pipeline/runner.go`
- `internal/pipeline/navigation.go`
- `internal/pipeline/status.go`
- Pipeline tests

Tests required:
- Stage order.
- Prerequisite matrix.
- Status transitions.
- Resume from persisted state.
- Event emission on transitions.

Docs updated:
- Architecture stage graph.
- Contracts stage/status section.

Spec update required:
- None unless stage semantics change.

Merge/integration checkpoint:
- No stage implementation should duplicate navigation logic.

Acceptance criteria:
- Empty fake stages can run through framework and persist progress.

### M6. Repo Analyze, Interview, Complexity, Design, Hivemind, Validate Stages

Status: in_progress. M6-REPO-ANALYZE was implemented by Repo Analysis Worker on 2026-06-30. M6-INTERVIEW-COMPLEXITY was implemented by Interview/Complexity Worker on 2026-06-30. M6-DESIGN was implemented by Design Worker on 2026-06-30. M6-HIVEMIND-VALIDATE was implemented by Hivemind/Validation Worker on 2026-06-30; full runner/app integration through planning remains follow-up.

Evidence for M6-REPO-ANALYZE:
- `internal/pipeline/repo_analyze.go` implements deterministic `repo_analyze` as a `PipelineStage` with `Name`, `Validate`, `Run`, `Resume`, and `StageOutputter` support.
- The stage uses an explicit project-root constructor, bounded file/context caps, default excludes for `.git`, `.nexdev`, `node_modules`, `vendor`, `dist`, and `build`, secret-file avoidance for `.env` and key-like files, and heuristic detection for languages, frameworks, package managers, test/lint commands, entrypoints, important files, untrusted repo instructions, risk notes, and summary.
- The stage writes `.nexdev/artifacts/repo_analysis.json` and indexes it through `Store.UpsertArtifact` when `StageEnv.Store` is a `*state.Store` and project/run identifiers are available.
- `internal/pipeline/repo_analyze_test.go` covers bounded excludes, detection heuristics, untrusted instruction capture, secret file avoidance, artifact JSON writing/indexing, and stage interface/resume behavior.

Evidence for M6-INTERVIEW-COMPLEXITY:
- `internal/pipeline/interview.go` implements provider-backed `interview` as a `PipelineStage` using `provider.StructuredClient` and `provider.SlotInterview`, trusted/untrusted prompt sections, semantic validation for `contract.InterviewData`, yes/CI assumption conversion, `BlockedError` for unresolved open questions, `.nexdev/artifacts/interview.json` writing, and optional state artifact indexing.
- `internal/pipeline/complexity.go` implements deterministic-first `complexity` as a `PipelineStage`, optional `provider.SlotComplexity` refinement, deterministic floor enforcement for score/level/phases/risks/voices/suggested tests, `.nexdev/artifacts/complexity_profile.json` writing, and optional state artifact indexing.
- `internal/pipeline/interview_test.go` and `internal/pipeline/complexity_test.go` cover valid fake-provider structured calls, underspecified blocked behavior, yes/CI assumptions, deterministic complexity levels, provider refinement, invalid structured repair, artifact writing/indexing, and stage interface behavior.

Evidence for M6-DESIGN:
- `internal/pipeline/design.go` implements provider-backed `design` as a `PipelineStage` using `provider.StructuredClient` and `provider.SlotDesign`, trusted/untrusted prompt sections, local response/metadata structs, a bounded default-three-iteration critique/correction loop, required design heading validation, `BlockedError` for unresolved high/critical actionable findings unless risk is accepted, `.nexdev/artifacts/design_draft.md` writing, and optional state artifact indexing.
- `internal/pipeline/design_test.go` covers successful design artifact writing/indexing, correction loop feedback and stopping, max-iteration blocking for high-severity remaining findings, invalid structured-output repair, required section validation, and stage interface/resume/output behavior.

Evidence for M6-HIVEMIND-VALIDATE:
- `internal/pipeline/hivemind.go` implements provider-backed `hivemind` as a `PipelineStage` using `provider.StructuredClient`, `provider.SlotHivemindVoice`, `provider.SlotHivemindSynthesis`, configured voices, sequential default execution, bounded parallel execution when configured, trusted/untrusted prompt sections, semantic validation for `contract.HivemindCritique` and `contract.HivemindSynthesis`, `.nexdev/artifacts/design_review.json` writing, optional state artifact indexing, and `BlockedError` for synthesis `revise` or `block` after artifact persistence.
- `internal/pipeline/validate.go` implements provider-backed `validate` as a `PipelineStage` using `provider.StructuredClient` and `provider.SlotValidate`, supplied interview/repo-analysis/complexity/design/hivemind synthesis inputs, semantic validation for `contract.ValidationReport`, `.nexdev/artifacts/validation_report.json` writing, `.nexdev/artifacts/validated_design.md` writing for pass/warn, optional state artifact indexing, and default blocking on conflicts, blockers, and block verdicts without deleting requirements.
- `internal/pipeline/hivemind_test.go` and `internal/pipeline/validate_test.go` cover fake-provider voice/synthesis/validation calls, invalid structured-output repair, revise/block behavior, pass/warn/block behavior, conflict/blocker default blocking, artifact writing/indexing, bounded parallel hivemind order preservation, and stage interface/resume/output behavior.

Next action guidance:
- Runner/app integration should pass the design draft output/artifact plus `contract.RepoAnalysis`, `contract.InterviewData`, `contract.ComplexityProfile`, and latest `contract.HivemindSynthesis` into concrete stage constructors rather than rescanning or regenerating them independently.
- M7/shared artifact work should factor artifact writing/indexing and add `artifact_updated` emission after persistence so later stages do not duplicate local helpers.
- M6/M15 security follow-up should decide whether repo_analyze/interview/complexity/design prompt-injection findings should also persist `security_warning` events once event emission ownership for concrete stages is assigned.
- State follow-up should add typed repositories for `hivemind_results` and `validate_results`; M6-HIVEMIND-VALIDATE intentionally wrote/indexed artifacts only because state edits were out of scope.

Purpose:
- Implement the pre-planning analysis and design stages.

Prerequisites:
- M5 runner.
- M4 fake provider.
- M3 artifacts/state.

Subagents that can run in parallel:
- Repo Analysis Worker.
- Interview/Complexity Worker.
- Design Worker.
- Hivemind/Validation Worker after design output contract is stable.

Contracts required before starting:
- `RepoAnalysis`, `InterviewData`, `ComplexityProfile`, design artifact metadata, `HivemindCritique`, `HivemindSynthesis`, `ValidationReport`.

Expected files touched:
- `internal/pipeline/repo_analyze.go`
- `internal/pipeline/interview.go`
- `internal/pipeline/complexity.go`
- `internal/pipeline/design.go`
- `internal/pipeline/hivemind.go`
- `internal/pipeline/validate.go`
- Stage fixtures/goldens

Tests required:
- Bounded repo analysis.
- Interview assumptions in `--yes`/CI mode.
- Complexity deterministic heuristics.
- Design correction loop max iterations.
- Hivemind parallel voices with cost guard.
- Validation blocks conflicts/blockers.
- Fake provider pre-planning integration.

Docs updated:
- Stage reference in architecture.
- Test fixtures in testing strategy.

Spec update required:
- Spec-management after milestone to record any prompt/schema clarifications.

Merge/integration checkpoint:
- Verify artifacts are written and indexed.

Acceptance criteria:
- Fake provider completes stages through `validate`.
- High-severity unresolved design findings block unless risk accepted.

### M7. Plan Sketch, Plan Detail, Review, Artifact Generation

Status: planning and review gate service/stage implemented and package-verified on 2026-06-30; review UI/control-plane event integration remains follow-up work.

Evidence:
- `internal/pipeline/plan_sketch.go` implements provider-backed `plan_sketch` with `provider.SlotPlanSketch`, strict structured `[]contract.PhaseSketch`, validation repair, canonical phase numbering/IDs, deduplication, and `.nexdev/artifacts/devplan.json` phase sketch output.
- `internal/pipeline/plan_detail.go` implements provider-backed `plan_detail` with `provider.SlotPlanDetail`, strict structured `[]contract.TaskSpec`, validation for acceptance criteria, write-task expected files, dependency references, and dependency cycles, deterministic `devplan.json`, `devplan.md`, and `phaseNNN.md` rendering, artifact indexing, and pending `nexdev_tasks` persistence with stable plan version/order.
- Tests in `internal/pipeline/plan_sketch_test.go` and `internal/pipeline/plan_detail_test.go` cover phase numbering/deduplication, validation failures, dependency cycle rejection, artifact rendering/indexing, state task persistence, invalid structured repair, and stage interface/resume behavior with the fake provider.
- `internal/pipeline/review.go` implements review modes `manual`, `auto`, `ci`, and explicit-allowance `skip` over persisted pending `nexdev_tasks`; approval writes `.nexdev/artifacts/review_approval.json` and stage output marker `reviewed_approved_plan`; update/delete APIs reject non-pending unsafe edits, increment plan version, and write `plan_edit_events` with actor and patch metadata.
- Tests in `internal/pipeline/review_test.go` cover manual blocking, approval/resume marker behavior, edit version increments and `plan_edit_events`, delete pending task safety, non-pending edit rejection, high-risk CI rejection without tests, skip allowance, and auto approval marker indexing.
- Evidence command: `go test ./internal/pipeline` passed.

Next action guidance:
- M8 develop prerequisite wiring should require the `reviewed_approved_plan` marker from review stage output or `.nexdev/artifacts/review_approval.json`; it must not treat pre-review `pending` tasks as approved.
- App/runner wiring should pass `PlanSketchStage.Phases()` into `PlanDetailStage` or load equivalent artifact output once artifact handoff wiring exists.
- Control-plane/event follow-up should route task mutation and approval endpoints through `ReviewService`, enforce admin/operator roles, and emit `plan_updated`, `review_required`, `review_completed`, and `artifact_updated` events after persistence through the eventual shared artifact/event helper.
- M12/TUI follow-up should implement review UI as a client over the same service semantics rather than owning plan state; future state refinement can add first-class task approval columns to replace the current deterministic marker artifact.

Purpose:
- Generate high-level phases, detailed tasks, human-readable plans, and review/edit/version flow.

Prerequisites:
- M6 validate.
- M3 plan edit/artifact state.

Subagents that can run in parallel:
- Planning Worker for `plan_sketch`.
- Planning Worker for `plan_detail` after phase sketch contract.
- Review Worker.
- Documentation Worker for artifact docs.

Contracts required before starting:
- `PhaseSketch`.
- `TaskSpec`.
- Plan DAG validation.
- Plan edit event schema.
- Artifact manifest schema.

Expected files touched:
- `internal/pipeline/plan_sketch.go`
- `internal/pipeline/plan_detail.go`
- `internal/pipeline/review.go`
- Artifact writer helpers if separate
- Golden fixtures for `devplan.md` and phase files

Tests required:
- Phase numbering/deduplication.
- Task acceptance criteria required.
- Expected files required for write tasks.
- Dependency cycles rejected.
- File overlap detection for parallel workers.
- Review edit increments plan version and writes `plan_edit_events`.

Docs updated:
- Artifact contracts.
- Pipeline stage docs.

Spec update required:
- Record any review skip semantics decision.

Merge/integration checkpoint:
- Orchestrator verifies planned tasks are stable before develop/executor work.

Acceptance criteria:
- Fake provider produces `devplan.json`, `devplan.md`, and phase artifacts.
- Review gate approval state satisfies develop prerequisites.

### M8. Executor Integration, Steering, Live Task Context

Status: M8-DEVELOP-EXECUTOR verified on 2026-06-30 for the safe fake-worker develop bridge and review approval prerequisite. Real LLM code execution, shell/network tools, control-plane handlers, full steering prompt context, project-lock lifecycle wiring, and changed-file manifests remain follow-up work.

Evidence:
- `internal/executor.NexdevExecutor` lists pending/pending-after-detour `nexdev_tasks`, updates status through state, persists mapped task events with `Store.PersistEvent`, creates `nexdev_blockers` for worker blockers, exposes pause/resume/cancel/skip/current-task/steering controls, and returns `TaskReport` values.
- `internal/executor.FakeWorker` deterministically emits progress/completion/blocker updates and can write files only when they match task `ExpectedFiles` and pass `internal/safety.PathSanitizer`. It does not execute shell commands, network calls, or real LLM code.
- `internal/pipeline.DevelopStage` requires `.nexdev/artifacts/review_approval.json` with `reviewed_approved_plan` before execution and leaves pre-review pending tasks untouched when the marker is missing.
- Evidence commands: `go test ./internal/executor ./internal/pipeline` passed; `go test ./...` passed.

Next action guidance:
- Control-plane and CLI workers can wire pause/resume/skip/cancel/steer to `executor.Control` without adding handler behavior inside `internal/executor`.
- Steering follow-up should enrich prompt context from requirements, design, plan notes, acceptance criteria, expected files, and bounded repo context while preserving `SafetyPolicyOverrideAllowed=false`.
- Git/verify follow-ups should populate changed-file manifests and task report changed files; shell/network execution must remain absent until an explicitly assigned policy-gated tool runner exists.
- App lifecycle follow-up should acquire the project lock before mutating develop runs.

Purpose:
- Bridge approved plans into controlled task execution.

Prerequisites:
- M7 reviewed plan.
- M4 provider/fake worker.
- M2 safety.
- M3 state/events.

Subagents that can run in parallel:
- Executor/Steering Worker A: executor controls.
- Executor/Steering Worker B: task update event bridge.
- Executor/Steering Worker C: prompt context builder and steering.
- Git Worker: changed-file detection support.

Contracts required before starting:
- Executor interface.
- Task status enum.
- Steering contracts.
- Safety policy interface.

Expected files touched:
- `internal/executor/*`
- `internal/pipeline/develop.go`
- `internal/steering/*`
- `internal/git/changed_files.go`

Tests required:
- Fake task execution.
- Task update event mapping.
- Pause/resume/cancel/skip.
- Steering changes next prompt context.
- Safety rejects unexpected writes.

Docs updated:
- Executor/steering contracts.
- Architecture flow.

Spec update required:
- None unless geoffrussy executor requires API adaptation.

Merge/integration checkpoint:
- Develop must not run unsafe shell commands by default.

Acceptance criteria:
- Fake develop can execute one approved task safely and persist events/status.

### M9. Detour and Blocker/Deblocker Workflow

Status: M9-DETOUR-WORKFLOW implemented and package-verified on 2026-06-30. M9-DEBLOCK-DENSE-ORDER resolved the dense-plan ordering blocker on 2026-06-30 with safe state-owned order shifting. Control-plane route/UI, optional operator approval, and automatic develop resume remain follow-up work.

Evidence:
- `internal/detour/manager.go` adds `WorkflowManager`, captures trigger task/blocker/neighbor/design/repo/depth context, calls `provider.StructuredClient` through `provider.SlotPlanDetail`, validates generated `TaskSpec` rows, enforces max depth, creates `detour_depth_exceeded` blockers, persists detour records, marks triggers `pending_after_detour`, and persists `detour_created` events.
- `internal/detour/splice.go` adds deterministic immediate splice ordering after the trigger, detour ID conflict detection, and state-facing inserted task order metadata.
- `internal/state/tasks.go` adds `InsertNexdevTasksAfter`, which transactionally shifts later tasks in the same run and plan version before inserting detour rows so dense `plan_order` values satisfy immediate splice semantics without violating uniqueness.
- `internal/detour/manager_test.go` and `internal/detour/splice_test.go` cover blocker-triggered request flow, task validation, dense and gapped splice ordering, ID conflict detection, depth-exceeded blocker/no silent skip, fake-provider structured repair, detour record persistence, event persistence, and trigger status updates.
- `internal/state/tasks_test.go` covers dense-plan insertion with multiple detour tasks, dependency validation across inserted tasks, stable relative order for existing later tasks, and stable plan version metadata.
- Evidence commands: `go test ./internal/state ./internal/detour` passed; `go test ./...` passed.

Next action guidance:
- M10 control-plane should expose `POST /detour` by calling `WorkflowManager.Request`/`RequestForBlocker` through an authenticated operator/admin service path and should broadcast only persisted events.
- M12 CLI/TUI should present detour context/result previews as clients of the same service path, not by mutating plan state directly.
- Dense-order splicing is now state-owned through `InsertNexdevTasksAfter`; future review/control-plane task mutation paths should reuse state-owned reorder semantics rather than implementing ad hoc plan-order shifts.
- Provider/config follow-up should decide whether to add a dedicated detour provider slot; M9 intentionally used `provider.SlotPlanDetail` without changing provider contracts.

Purpose:
- Implement first-class detours and blocker flow.

Prerequisites:
- M8 executor.
- M7 plan/task contracts.
- M4 provider.

Subagents that can run in parallel:
- Detour Worker A: contracts/splice.
- Detour Worker B: provider flow.
- Detour Worker C: blocker integration.

Contracts required before starting:
- `DetourRequest`, `DetourResult`.
- Blocker schema.
- Plan versioning contract.

Expected files touched:
- `internal/detour/*`
- `internal/pipeline/develop.go` integration
- Detour tests

Tests required:
- Blocker creates detour.
- Detour tasks validate with task schema.
- Splice after trigger task.
- Max depth exceeded creates blocker and pauses.

Docs updated:
- Detour workflow in architecture/contracts.
- Worker/deblocker protocol remains synchronized.

Spec update required:
- Spec-management validates detour semantics after milestone.

Merge/integration checkpoint:
- No worker may hack around blockers; blocked flow must use detour/deblocker path.

Acceptance criteria:
- Fake blocked task produces detour tasks and resumes develop when policy allows.

### M10. Control-Plane HTTP/SSE/Auth APIs

Status: M10-CONTROL-PLANE implemented and package-verified on 2026-06-30 for local HTTP routing, role auth middleware, durable-state read routes, persisted SSE replay, and M9 detour route integration. CLI `nexdev serve`, token command UX, generated OpenAPI server types, MCP dispatch, task/config/provider mutation services, and auth audit/rate limiting remain follow-up work.

Evidence:
- `internal/controlplane/server.go`, `routes.go`, `errors.go`, `events.go`, and `sse.go` implement stdlib ServeMux routing, OpenAPI-shaped JSON errors, CORS allow-list behavior, durable status/plan/artifact/event read routes, SSE replay with `Last-Event-ID`, heartbeat comments, bounded publisher queues, and persist-before-broadcast publisher semantics.
- `internal/controlplane/auth.go` now implements opaque bearer token helpers, HMAC-SHA256 token hashing with a server secret, token repository authentication, expiry/revocation rejection, role middleware, and `last_used_at` updates after successful auth.
- `POST /detour` delegates to an injected M9-compatible requester (`WorkflowManager.Request`/`RequestForBlocker`) and does not implement ad hoc ordering or provider calls. Pause/resume/skip/steer/cancel are thin adapters over injected `executor.Control` and return service-unavailable until app wiring supplies the control service.
- `internal/controlplane/server_test.go` covers loopback no-auth read behavior, non-loopback no-auth startup failure, authenticated observer/operator route behavior, forbidden observer mutation, durable SSE replay after `Last-Event-ID`, SSE frame shape/no `[DONE]`, and detour route delegation with persisted `detour_created` evidence.
- Evidence command: `go test ./internal/controlplane` passed.

Next action guidance:
- M12 CLI/app wiring should construct `controlplane.Server` with concrete state, executor control, detour workflow manager, run starter, project ID, config-derived bind/auth/CORS/SSE settings, and project-lock lifecycle before exposing `nexdev serve`.
- Auth CLI work should add token create/list/revoke UX using `GenerateOpaqueToken`, `HashBearerToken`, and the existing state token repository without returning plaintext tokens after creation.
- M11 MCP work should use the same role hierarchy and service paths rather than bypassing HTTP/control-plane semantics.
- A contract/API follow-up can introduce generated OpenAPI server types and response validation; current M10 manually binds handlers to the frozen `api/openapi.yaml` paths.
- M15 should add auth audit/rate limiting and slow-client stress coverage for queue-overflow behavior.

Purpose:
- Expose run state and live control over HTTP/SSE with role enforcement.

Prerequisites:
- M1 OpenAPI.
- M2 config/security baseline.
- M3 state/event log/auth tables.
- M5 runner service interfaces.
- M8 executor controls.
- M9 detour for detour route.

Subagents that can run in parallel:
- Control Plane Worker A: server/routing/error shape.
- Control Plane Worker B: SSE replay manager.
- Auth/Security Worker: auth/role middleware and token commands integration.
- API Contract Worker: OpenAPI tests/codegen drift.

Contracts required before starting:
- OpenAPI route schemas.
- Auth role matrix.
- Event repository.
- Service interfaces.

Expected files touched:
- `internal/controlplane/*`
- Generated API files
- API tests
- SSE tests

Tests required:
- Health/status/plan/artifacts/events routes.
- Mutation route role checks.
- Auth token create/list/revoke.
- SSE replay/heartbeat/slow-client.
- ErrorResponse shape.
- Startup fails remote bind without auth.

Docs updated:
- `docs/contracts.md` API/SSE/auth.
- Create/update `docs/API.md` when route behavior stabilizes.
- Create/update `docs/SECURITY.md` auth section.

Spec update required:
- Record auth storage locality and `auth_required: auto` resolution.

Merge/integration checkpoint:
- Orchestrator validates OpenAPI and server behavior before CLI remote/MCP build.

Acceptance criteria:
- `nexdev serve` can expose health/status/events/SSE with required auth behavior once CLI wires it.

### M11. MCP-Compatible Tool Surface

Purpose:
- Expose MCP tools as thin, permissioned wrappers around control-plane operations.

Prerequisites:
- M10 control plane/auth.
- M1 OpenAPI schemas.

Subagents that can run in parallel:
- MCP Worker for tool manifest.
- MCP Worker for call dispatcher.
- Security Worker for MCP poisoning fixtures.

Contracts required before starting:
- MCP tool list.
- Role mapping.
- Input schemas.

Expected files touched:
- `internal/controlplane/mcp.go`
- `api/mcp_tools.json`
- MCP tests

Tests required:
- Required tools listed.
- Tool inputs validate.
- Same role checks as HTTP.
- Poisoned tool description cannot expand permission.

Docs updated:
- MCP section in `docs/contracts.md` and `docs/API.md`.

Spec update required:
- None unless MCP transport scope changes.

Merge/integration checkpoint:
- MCP must not become an alternate unpermissioned execution path.

Acceptance criteria:
- MCP start/status/plan/artifacts/pause/resume/cancel/steer/detour/resolve/provider-test tools work against fake run state.

Status: M11-MCP-SURFACE implemented and package-verified on 2026-06-30 for the control-plane MCP-compatible tool surface.

Evidence:
- `internal/controlplane/mcp.go` defines the required static `nexdev_*` descriptors, per-tool role metadata, strict argument validation, and `/mcp/call` dispatch.
- MCP read tools use durable control-plane/state paths for status, plan, and artifact index metadata.
- MCP workflow tools delegate to existing services: `RunStarter`, `executor.Control`, `DetourRequester.Request`/`RequestForBlocker`, durable blocker resolution, and optional `ProviderTester`.
- The MCP layer does not call providers directly, execute shell commands, or implement detour/task ordering.
- Legacy `internal/mcp` stdio registration is disabled so imported direct provider/executor handlers are not exposed by the existing CLI path.
- Evidence commands: `go test ./internal/controlplane` passed; `go test ./internal/mcp` passed.

Next action guidance:
- App/CLI workers should expose `/mcp/tools` and `/mcp/call` through the M10 server rather than wiring legacy `internal/mcp` directly; a later stdio adapter must call the M11 control-plane dispatcher/service boundary.
- Artifact/API workers should add a project-root validated artifact content reader before `nexdev_get_artifact` returns file contents.
- Provider/control-plane workers should inject a real `ProviderTester` service; until then the MCP provider-test tool returns a structured deferred error.

### M12. CLI Commands and Operator Workflow

Status: M12-CLI-APP-WIRING implemented and package-verified on 2026-06-30 for control-plane server lifecycle, safe local defaults, project lock lifecycle, token UX, app-owned detour workflow wiring, and initial client adapters. Full run/fake-provider E2E, verify/handoff commands, live `events --follow`, and provider-test execution service remain follow-up work.

Evidence:
- `internal/app/lifecycle.go` opens project-local Nexdev runtime state, loads `nexdev.yaml` with CLI overrides, creates stable project IDs, ensures project rows, manages project-local server secrets, creates hash-only auth tokens, builds the M10/M11 control-plane server, and acquires/releases `.nexdev/run/project.lock` for `serve`.
- `internal/cli/root.go` now exposes the `nexdev` root command and required global flags: `--project-dir`, `--config`, `--state-dir`, `--no-tui`, `--json`, `--log-level`, `--profile`, `--control-url`, and `--token`.
- `internal/cli/m12_commands.go` adds `run`, `verify`, `history`, `serve`, `auth token create|list|revoke`, `events`, `detour`, `steer`, `pause`, `cancel`, `blockers`, `provider`, `artifacts`, and `doctor` command wiring over app/control-plane boundaries. Existing `resume` uses `POST /resume` when `--control-url` is set; `status --json` reads through the control-plane handler. Commands whose lower-level services are not ready return explicit deferred errors instead of bypassing service boundaries.
- M12 tests cover command registration, token creation output, project lock acquire/release, remote-bind/no-auth rejection before server construction, hash-only token storage, and CLI mutation refusal without `--control-url`.
- Evidence commands: `go test ./internal/app ./internal/cli ./internal/controlplane` passed; `go test ./internal/git ./internal/config` passed.

Next action guidance:
- M16/E2E should wire full `run --fake-provider --no-tui --json`, live `events --follow`, verify/handoff, and fake-provider E2E after the lower-level run/verify/handoff services exist.
- Provider/control-plane follow-up should inject a `ProviderTester` service. Detour generation is now wired through app-owned provider router construction and M9 `detour.WorkflowManager`; provider configuration/auth failures surface through that service path rather than direct provider calls from CLI.
- A future CLI cleanup task should retire or adapt imported geoffrussy-era command bodies that still read `.geoffrussy` state for interactive legacy flows.

Purpose:
- Provide the complete operator command surface.

Prerequisites:
- M2 config.
- M5 runner.
- M7 review/plan.
- M8 develop.
- M10 control plane/auth.

Subagents that can run in parallel:
- CLI Worker A: root/global flags/config/status.
- CLI Worker B: run/develop/verify/review/navigation commands.
- CLI Worker C: control commands pause/resume/steer/detour/cancel/blockers.
- CLI Worker D: provider/events/artifacts/history/auth/serve/doctor.

Contracts required before starting:
- Command list and semantics.
- Local service interfaces.
- Remote client interfaces.

Expected files touched:
- `cmd/nexdev/*`
- `internal/cli/*` if adopted
- CLI tests

Tests required:
- Command help and flag tests.
- `config validate`.
- `doctor`.
- `status --json`.
- `events --follow` with fake server.
- Token commands.
- `run --fake-provider --no-tui --json` by M16.

Docs updated:
- README quickstart once stable.
- Configuration and operations docs.

Spec update required:
- Record any command naming deviation.

Merge/integration checkpoint:
- CLI commands must call in-process services locally and HTTP client only with `--control-url`.

Acceptance criteria:
- Required command tree exists and smoke tests pass for implemented dependencies.

### M13. TUI/Web/Control UX If In Scope

Status: M13-TERMINAL-TUI implemented and package-verified on 2026-06-30 for terminal TUI/control UX only. Embedded web UI is deferred and no `web/static` files were created.

Evidence:
- `internal/tui/nexdev.go` adds a Nexdev terminal control model over a narrow `tui.Client` service boundary, with HTTP and in-process control-plane handler clients. It reads run status, events, plan/tasks, blockers, artifacts, redacted config, and provider summaries through service/client calls rather than owning pipeline state.
- The model implements overview, event stream, plan/task, blocker/detour, and artifact/config/provider summary views; `1`-`5` navigation; `r` refresh; `q` quit; `p` pause/resume; `s` steer; `d` detour; `k` skip with confirmation; and `c` cancel with confirmation.
- Disabled/deferred services surface as structured status/errors from the injected client path. The TUI does not call providers, execute shell commands, or mutate task/plan ordering directly. Normal quit exits only the TUI and does not cancel a run.
- Rendered run/task/artifact/event/blocker text is redacted and control-character scrubbed before display.
- `nexdev tui` launches the terminal client. With `--control-url`, it uses the HTTP control-plane client; without it, it opens the app runtime, acquires the project lock, constructs the existing control-plane handler, and talks through that handler.
- Evidence commands so far: `go test ./internal/tui` passed; `go test ./internal/cli` passed.

Next action guidance:
- Later review/control-plane work should expose plan edit/approval service methods before TUI inline plan editing is enabled; M13 intentionally renders that capability as deferred rather than mutating state directly.
- Later UX work can add text-entry dialogs for steering/detour/review edits over the same `tui.Client` boundary.
- Web UI remains deferred until explicitly assigned; do not add `web/static` assets without orchestrator approval.

Purpose:
- Implement terminal control UX and optional embedded web UI only if in scope after M12.

Prerequisites:
- M10 control plane/events.
- M12 CLI app wiring.
- M7 review plan editing.
- M9 detour.

Subagents that can run in parallel:
- TUI Worker A: app/run overview.
- TUI Worker B: event stream.
- TUI Worker C: plan editor.
- TUI Worker D: detour/provider/config/artifact views.

Contracts required before starting:
- TUI client/service model.
- Event subscription interface.

Expected files touched:
- `internal/tui/*`
- Optional `web/static/*` only with orchestrator approval

Tests required:
- TUI model tests.
- Keybinding tests.
- Plan edit service tests.
- Quit behavior test.

Docs updated:
- README and operations docs.

Spec update required:
- If optional web UI is deferred, spec-management records deferral.

Merge/integration checkpoint:
- TUI remains a client of core API/state and does not own pipeline state.

Acceptance criteria:
- Required TUI views/keybindings work against fake run state, or explicit deferral recorded.

### M14. Observability, Audit Logs, Cost/Usage Tracking

Status: M14-OBSERVABILITY-AUDIT-COST implemented and targeted-package verified on 2026-06-30. Full run-summary aggregation, OTel exporter wiring, broad no-secret-leak E2E fixtures, and cost guard enforcement before parallel launches remain follow-up work.

Evidence:
- `internal/state/migrations.go` adds additive migration version `6` with `audit_log` and `cost_ledger` tables plus query indexes. `internal/state/audit_cost.go` adds repositories that redact string and JSON values before persistence.
- `internal/state/events.go` now decodes event payload JSON, recursively redacts string values, and persists the scrubbed payload so event boundaries cannot store raw secret-shaped values.
- `internal/provider.StructuredClient` now supports an optional `StructuredCallRecorder`, redacts previous raw responses before repair prompts, and emits redacted provider/model/usage/latency/error metadata without exposing prompts.
- `internal/observability` adds correlation context helpers, an optional `UsageRecorder` that persists provider usage/cost ledger records and audit entries through state, and inert OTel configuration that is disabled by default and performs no network/exporter setup in tests.
- `internal/controlplane.Authenticator` now writes durable audit records for failed auth, forbidden access, and allowed operator/admin control requests when auth is enabled and a project ID/store are available. Error response detail redaction now recurses through nested maps/slices.
- `internal/config.NexdevConfig` now includes typed `cost` and `observability.otel` defaults with OTel disabled by default and endpoint validation when enabled. `internal/app` wires app-created structured provider clients to the usage recorder with project/latest-run defaults.
- Evidence commands: `go test ./internal/state ./internal/provider ./internal/observability ./internal/controlplane ./internal/config ./internal/app` passed.

Next action guidance:
- Verify/handoff work should include cost/provider usage summaries from `cost_ledger` in `run_summary.json` once those artifacts exist.
- Pipeline/executor/app workers should pass `observability.ContextWithCorrelation` at provider/stage/task boundaries so cost/audit records get precise stage/task/request fields beyond current app defaults.
- M15 should add broader cross-boundary no-secret-leak integration fixtures for logs/events/API/audit/cost/artifacts and auth rate limiting.
- If OTel exporters are assigned later, keep them opt-in only, require explicit endpoint config, and avoid network access in normal tests.

Purpose:
- Complete traceable, redacted, auditable run metadata.

Prerequisites:
- M2 logging baseline.
- M3 state.
- M4 provider usage metadata.
- M8 executor events.
- M10 HTTP request IDs.

Subagents that can run in parallel:
- Observability Worker A: OTel traces/metrics.
- Observability Worker B: cost ledger.
- Security Worker: audit logging and redaction integration.

Contracts required before starting:
- Log fields.
- Cost ledger schema.
- Audit event schema.

Expected files touched:
- `internal/observability/*`
- State migrations if cost/audit tables added
- Provider/executor/control-plane instrumentation hooks

Tests required:
- Redacted structured logs.
- OTel disabled by default.
- Provider usage/cost records.
- Audit logs for auth failures and mutations.

Docs updated:
- Architecture observability.
- Security audit section.

Spec update required:
- If cost ledger table names differ from planned contracts, update contracts/spec.

Merge/integration checkpoint:
- No secret leakage in logs/events/artifacts.

Acceptance criteria:
- Run summaries include timings, provider usage, and cost estimates when configured.

### M15. Security Hardening and Prompt-Injection/Tool-Risk Defenses

Status: implemented and package-verified on 2026-06-30. Full fake-provider E2E no-secret-leak and slow-client stress remain M16/M19 follow-up.

Evidence:
- `internal/safety.ToolPolicy.ValidateTaskWritePath` enforces default write policy, deny globs, active file-lock globs, and task expected-file globs for owned helpers; shell/network execution remains denied and unimplemented by default.
- `internal/git.AcquireProjectLockWithPolicy` detects stale lock metadata deterministically and fails safe with `ErrProjectLockStale` without process probing or automatic lock deletion.
- `internal/controlplane` adds deterministic local auth throttling for authenticated routes, returns `429 rate_limited`, and writes `auth_throttle` audit records when audit storage is available.
- Pipeline artifact writing now redacts decoded JSON string values before persistence; Hivemind emits `security_warning` events for prompt-injection findings in untrusted repo context and checks a provider-launch cost guard before voice/synthesis calls.
- `internal/observability.CostGuard` denies configured budget-exceeded launches and optional unknown-price launches before provider execution.
- `docs/SECURITY.md` documents implemented security behavior and stale-lock recovery.

Evidence commands so far:
- `go test ./internal/safety` passed.
- `go test ./internal/controlplane` passed.
- `go test ./internal/git` passed.
- `go test ./internal/observability` passed.
- `go test ./internal/pipeline` passed.

Purpose:
- Validate safe local coding harness behavior before E2E completion.

Prerequisites:
- M2 safety baseline.
- M8 executor.
- M10 auth/control plane.
- M11 MCP.
- M14 redaction/audit.

Subagents that can run in parallel:
- Security Worker A: path/symlink/file lock hardening.
- Security Worker B: tool policy and command execution hardening.
- Security Worker C: prompt injection and MCP poisoning fixtures.
- Security Worker D: secret redaction audit.

Contracts required before starting:
- Tool policy schema.
- Redaction patterns.
- Prompt boundary rules.

Expected files touched:
- `internal/safety/*`
- Security tests/fixtures
- `docs/SECURITY.md`

Tests required:
- Prompt injection fixture.
- Malicious AGENTS fixture.
- Symlink escape.
- `.env` exfiltration.
- Unauthorized token role mutation.
- Command output cap.
- SSE slow client.
- MCP poisoning.

Docs updated:
- `docs/SECURITY.md` complete for implemented surface.
- `TESTING_STRATEGY.md` security tests.

Spec update required:
- Record any explicitly deferred sandbox behavior.

Merge/integration checkpoint:
- Security tests must pass before fake-provider E2E is accepted.

Acceptance criteria:
- Nexdev cannot be induced by repo/MCP/model text to override safety policy.

### M16. End-To-End Smoke Pipeline With Fake Provider

Status: implemented and package-verified on 2026-06-30 for deterministic local `nexdev run --fake-provider --no-tui --json`, verify/handoff artifacts, changed-file manifest, run summary, handoff markdown, persisted events, and `scripts/e2e_fake_provider.sh`. Broader release gates and real-provider execution remain later milestones.

Purpose:
- Prove full pipeline works deterministically without real provider access.

Prerequisites:
- M6-M12 complete.
- M15 critical security checks pass.

Subagents that can run in parallel:
- Test Infrastructure Worker: E2E script.
- Pipeline/Executor Workers: fix surfaced integration bugs within ownership.
- Docs Worker: operator smoke docs.

Contracts required before starting:
- Full fake provider fixture set.
- CLI run command.
- SSE endpoint.
- Artifact contracts.

Expected files touched:
- `scripts/e2e_fake_provider.sh`
- E2E fixtures
- Bugfix files only within assigned ownership

Tests required:
- `./scripts/e2e_fake_provider.sh`
- `nexdev run --fake-provider --no-tui --json`
- SSE replay verification.
- Artifacts and changed files exist.
- Run reaches `complete`.

Docs updated:
- README quickstart.
- Operating smoke-test docs.

Spec update required:
- Spec-management updates coverage matrix after fake-provider E2E passes.

Merge/integration checkpoint:
- This is the first full-system acceptance checkpoint.

Acceptance criteria:
- Fake-provider full run completes in a temp repo with replayable event stream and required artifacts.

### M17. Real Provider Integration Checks Behind Explicit Opt-In

Status: implemented and package-verified on 2026-06-30 for disabled-by-default real-provider smoke tests, skip-path coverage, HTTP/MCP provider-test service delegation, and opt-in script/docs. No real-provider tests were run in this environment.

Evidence:
- `internal/provider/real_smoke.go` implements explicit env gate parsing for `NEXDEV_RUN_REAL_PROVIDER_TESTS=1`, provider/model config, credential env selection, required spend cap `<= 0.25`, and timeout `<= 30s`.
- `internal/provider/real_smoke_test.go` verifies default skip behavior, missing credential/spend-cap skip behavior, unsafe cap/timeout rejection, provider-error redaction, and contains the only real network smoke test, which skips unless all gates are present.
- `scripts/real_provider_smoke.sh` is safe by default and runs skip-path tests unless the real-provider gate is explicitly set.
- `internal/controlplane` now delegates `POST /providers/{name}/test` to an injected `ProviderTester` and returns service-unavailable when absent; HTTP and MCP provider-test errors are redacted.
- `internal/app` injects the real-provider smoke tester only when `NEXDEV_RUN_REAL_PROVIDER_TESTS=1` is present.

Next action guidance:
- M18 spec-management should classify M17 as implemented without `SPEC.md` changes and keep real-provider checks optional for release-only environments.
- Release/CI work must not add these tests to normal PR CI with credentials; if release jobs run them, they must provide the explicit env gates and spend cap.
- A future provider task may add provider-specific cheapest-model recommendations, but must keep the current default skip behavior and no-secret guarantees.

Purpose:
- Validate provider plumbing without making CI costly or secret-dependent.

Prerequisites:
- M16 fake-provider E2E.
- M4 provider registry.
- M15 secret handling.

Subagents that can run in parallel:
- Provider Worker for opt-in smoke.
- Security Worker for env/secret validation.

Contracts required before starting:
- Real-provider env var gates.
- Spend cap.
- No-secret prompt fixtures.

Expected files touched:
- `scripts/real_provider_smoke.sh`
- Provider tests
- Docs

Tests required:
- Real provider tests skipped unless explicit env vars exist.
- Tiny structured call passes under spend cap when enabled.

Docs updated:
- Provider setup and opt-in smoke docs.

Spec update required:
- None unless supported providers differ from expectation.

Merge/integration checkpoint:
- Real-provider checks never block normal PR CI unless explicitly configured for release.

Acceptance criteria:
- Provider auth/list/basic structured call can be tested safely with explicit opt-in.

### M18. Documentation and Spec Stabilization

Status: implemented by M18-DOCS-SPEC-STABILIZATION on 2026-06-30 as a documentation-only reconciliation. No product behavior or `SPEC.md` changes were made.

Evidence:
- `README.md` now documents current fake-provider run/E2E, serve, token, events/status/artifact/provider reads, TUI, real-provider opt-in smoke, and known deferred commands.
- `docs/architecture.md`, `docs/contracts.md`, `docs/API.md`, `docs/SECURITY.md`, and `TESTING_STRATEGY.md` were reconciled through M17 behavior and M19 gaps.
- `SPEC_UPDATE_PROTOCOL.md` changelog records the M18 stabilization decision and the choice to leave `SPEC.md` unchanged.
- Spec coverage matrix statuses were reconciled through M17 with explicit remaining unbuilt sections below.

Remaining unbuilt requirements for M19/release follow-up:
- Generated OpenAPI server types and response/codegen drift checks.
- Slow-client SSE overflow stress coverage and event behavior validation.
- Policy-gated real verification command runner, output caps, controlled env, and repair loop.
- Standalone verify/handoff command behavior and richer CLI smoke, including `events --follow`.
- Full real-provider run execution; current real-provider support is smoke-only and opt-in.
- Broader hostile security fixtures beyond package-level coverage, including malicious repo docs and MCP poisoning at E2E depth.
- Web UI assets, if explicitly re-scoped; terminal TUI is implemented.
- Release/CI scripts, final handoff, and all full gates including `go test -race ./...` and `govulncheck ./...` in a prepared environment.

Purpose:
- Ensure docs, contracts, spec, tests, and implementation are synchronized.

Prerequisites:
- M16 complete.
- M17 optional or deferred.
- All major feature milestones either complete or explicitly deferred.

Subagents that can run in parallel:
- Documentation Worker.
- Spec Management Subagent.
- Testing Scout-style reviewer.
- Security reviewer.

Contracts required before starting:
- Final API/state/event/config/artifact contracts.

Expected files touched:
- `SPEC.md`
- `DEVPLAN.md`
- `README.md`
- `docs/*`
- `TESTING_STRATEGY.md`
- `SPEC_UPDATE_PROTOCOL.md`

Tests required:
- All docs-referenced commands are valid or clearly marked future/optional.
- Contract generation/checks pass.

Docs updated:
- All required docs.
- Spec coverage matrix.

Spec update required:
- No `SPEC.md` edit was made for M18; this stabilization records documentation/protocol decisions only. Final spec-management remains part of M19 release readiness if orchestrator-approved deferrals are needed.

Merge/integration checkpoint:
- No secondary doc contradicts `SPEC.md`.

Acceptance criteria:
- Secondary docs and the coverage matrix identify implemented behavior and remaining unbuilt requirements without contradicting `SPEC.md`.

### M19. Release Readiness

Status: M19 toolchain vulnerability deblock verified on 2026-06-30. The module and release configuration now require Go `1.26.4` for deterministic `govulncheck` release gates, and the local release gate passes with `govulncheck` on `PATH`.

Evidence added:
- `scripts/release_check.sh` runs OpenAPI/control-plane contract tests, `go test ./...`, `go test -race ./...`, `go vet ./...`, `go mod verify`, `govulncheck ./...` when installed, and `./scripts/e2e_fake_provider.sh` without running real-provider smoke.
- `.github/workflows/ci.yml` and `.github/workflows/release.yml` now target Go 1.26.4 and `cmd/nexdev` instead of geoffrussy-era binaries.
- `internal/controlplane` adds slow-client publisher overflow coverage for bounded subscriber queues.
- `docs/OPERATING.md` and `docs/RELEASE_READINESS.md` record release gate commands, optional real-provider smoke gates, OpenAPI drift status, and release blockers/follow-ups.
- Evidence commands: `PATH="/home/mojo/go/bin:$PATH" govulncheck ./...` passed; `PATH="/home/mojo/go/bin:$PATH" ./scripts/release_check.sh` passed.

Purpose:
- Validate v0.1 acceptance criteria and produce final maintainer handoff.

Prerequisites:
- M18 verified.

Subagents that can run in parallel:
- Release Worker.
- Test Infrastructure Worker.
- Security Worker.
- Documentation Worker.

Contracts required before starting:
- Final contract versions.

Expected files touched:
- CI/release scripts.
- Final handoff docs.
- Release notes if adopted.

Tests required:
- `go test ./...`
- `go test -race ./...`
- `go vet ./...`
- `govulncheck ./...`
- `go mod verify`
- `./scripts/e2e_fake_provider.sh`
- CLI smoke.
- Security fixtures.
- OpenAPI/codegen check.

Docs updated:
- Final handoff.
- Release/operations docs.

Spec update required:
- Final coverage matrix complete.

Merge/integration checkpoint:
- Orchestrator verifies no unresolved blockers.

Acceptance criteria:
- All devplan milestones complete or explicitly deferred.
- Every canonical spec requirement implemented or explicitly deferred.
- Tests pass.
- Fake-provider E2E passes.
- Security checks pass.
- Control-plane/API contracts tested.
- Docs match behavior.
- Final handoff exists.

## 8. Worker Protocol Summary

The full protocol is in `WORKER_PROTOCOL.md`. This section is duplicated here as the quick contract for the later orchestrator.

Every worker must:
- Read relevant `SPEC.md` sections.
- Read `DEVPLAN.md`.
- Read `WORKER_PROTOCOL.md`.
- Read `SPEC_UPDATE_PROTOCOL.md`.
- Read `TESTING_STRATEGY.md` if writing tests.
- Inspect only owned files plus explicitly allowed contracts.
- Create or update tests with implementation.
- Run the relevant test subset.
- Update progress in `DEVPLAN.md`.
- Update docs for changed behavior.
- Never modify unrelated domains.
- Stop immediately on blocker.
- Return a handoff note.

Worker completion report format:
- Task ID.
- Summary.
- Files changed.
- Tests added or changed.
- Tests run.
- Tests skipped with reason.
- Docs updated.
- Spec impact.
- Open risks.
- Next recommended task.
- Blocker-free: yes/no.

Worker file-boundary rule:
- If a required edit touches another domain's file, stop and ask the orchestrator to coordinate or reassign.

## 9. Spec-Management Protocol Summary

The full protocol is in `SPEC_UPDATE_PROTOCOL.md`.

After every major milestone:
1. Orchestrator spawns Spec Management Subagent.
2. It compares completed work against `SPEC.md`.
3. It updates `SPEC.md` only for implemented accepted behavior, approved deviations, clarifications, or explicit deferrals.
4. It must not weaken requirements to hide incomplete work.
5. It records milestone completed, implementation evidence, spec sections updated, deviations approved, and remaining unbuilt spec sections.
6. It updates the spec coverage matrix.

The project is not complete until:
- Every devplan task is complete or explicitly deferred.
- Every canonical spec requirement is implemented or explicitly deferred in the spec.
- Tests pass.
- Docs match behavior.
- Spec coverage is complete.

## 10. Blocker and Deblocker Workflow

When a builder subagent hits a blocker:

1. Stop immediately.
2. Do not guess.
3. Do not hack around the blocker.
4. Write a blocker handoff containing what it was trying to do, relevant files, exact error/output, hypotheses checked, constraints, minimal reproduction steps, and recommended deblocker specialization.
5. Return control to the orchestrator.
6. Orchestrator spawns a specialized Deblocker Subagent.
7. Deblocker investigates only the blocker.
8. Deblocker returns findings and recommended resolution.
9. Orchestrator assigns the resolution to the original or replacement builder.
10. Builder continues only after deblocker report is accepted.

Specialized deblocker types:
- Go compile/test deblocker.
- SQLite/migration deblocker.
- SSE/control-plane deblocker.
- Provider/LLM API deblocker.
- Security/auth deblocker.
- Concurrency/race deblocker.
- Git/worktree deblocker.
- Flaky-test deblocker.
- Contract mismatch deblocker.
- Docs/spec ambiguity deblocker.

Blocker handoff template:

```markdown
## Blocker Handoff

Task ID:
Worker role:
Goal:
Owned files:
Relevant files:
Exact error/output:
Reproduction steps:
Hypotheses checked:
Constraints:
Recommended deblocker:
Risk if bypassed:
```

## 11. Test Strategy Summary

The full strategy is in `TESTING_STRATEGY.md`.

Required package-level tests:
- Config loading and validation.
- Path sanitizer.
- Redaction.
- Role checks.
- Event log replay.
- SSE formatting.
- Stage prerequisite validation.
- Plan DAG validation.
- Detour splicing.
- Steering context summarization.
- Provider structured-output repair.
- SQL migrations.

Required integration tests:
- Fake provider full pipeline.
- Fake provider invalid JSON repair.
- Pause/resume/skip during develop.
- SSE reconnect with `Last-Event-ID`.
- Auth role matrix.
- Blocker to detour to resume.
- Review plan edit to plan version increment.
- Verify failure to repair attempt.
- Migration from geoffrussy state.

Required E2E:
- `./scripts/e2e_fake_provider.sh` creates temp repo, runs `nexdev init`, runs full fake-provider pipeline, connects SSE client, verifies replay, verifies artifacts, verifies changed files, verifies run reaches `complete`.

Required security fixtures:
- Prompt injection README.
- Malicious `AGENTS.md`.
- Symlink escape write.
- `.env` exfiltration attempt.
- Unauthorized role mutation.
- Unbounded command output.
- SSE slow client overflow.
- MCP tool description poisoning.

Required commands after implementation:
- `go test ./...`
- `go test -race ./...`
- `go vet ./...`
- `govulncheck ./...`
- `go mod verify`
- `./scripts/e2e_fake_provider.sh`

Optional real-provider smoke:
- Must be disabled by default.
- Must require explicit env opt-in.
- Must use tiny prompts.
- Must enforce spend cap.
- Must never send repo secrets.

## 12. Best-Practice Package Recommendations

| Concern | Decision | Package/Approach | Why | Complexity | Adopting Worker |
|---|---|---|---|---|---|
| CLI | Adopt | `github.com/spf13/cobra` | Matches geoffrussy and required command surface | Low | CLI Worker |
| TUI | Adopt if geoffrussy present | Bubble Tea, Bubbles, Lipgloss | Matches spec and existing foundation | Medium | TUI Worker |
| HTTP routing | Adopt | stdlib `net/http` `ServeMux` | Enough for v0.1, avoids framework bloat | Low | Control Plane Worker |
| OpenAPI codegen | Adopt | `github.com/oapi-codegen/oapi-codegen/v2` | Prevents route/type drift | Medium | Contract/API Worker |
| OpenAPI validation | Adopt in tests/dev | `github.com/getkin/kin-openapi` | Catches contract drift | Low-Medium | Contract/API Worker |
| SQLite driver | Adopt initially | `github.com/mattn/go-sqlite3` | Mature and likely geoffrussy-compatible | Low | State Worker |
| Typed SQL | Adopt unless base says no | `sqlc` | Reduces runtime scan errors | Medium | State Worker |
| Migrations | Adopt existing or Goose | Existing geoffrussy runner or `github.com/pressly/goose/v3` | Keep simple, embedded migrations | Low-Medium | State Worker |
| Logging | Adopt | stdlib `log/slog` | Standard, structured, redaction-friendly | Low | Observability Worker |
| Telemetry | Adopt optional | OpenTelemetry Go | Traces/metrics for stages/providers/tools | Medium | Observability Worker |
| Vulnerability scanning | Adopt | `govulncheck` | Required by spec | Low | Release Worker |
| Secrets storage | Defer | OS keyring library | Only needed if spec expands persisted secret storage | Medium | Security Worker |
| CGO-free SQLite | Defer | `modernc.org/sqlite` | Release optimization, not v0.1 default | Medium | Release Worker |
| Heavy web framework | Reject | Gin/Echo/Fiber/Chi | Unneeded for v0.1 with stdlib + OpenAPI | Avoided | Control Plane Worker |
| ORM | Reject | GORM/ent | Less auditable than typed SQL | Avoided | State Worker |
| Queue framework | Reject | Redis/NATS/Temporal | Single-project local harness should stay SQLite-backed | Avoided | Orchestration Worker |
| Homegrown JWT | Reject | Custom JWT/JWS | Too easy to get wrong; opaque tokens default | Avoided | Auth Worker |

## 13. Documentation Requirements

Required docs maintained continuously:
- `README.md`: quickstart, current status, commands, links.
- `DEVPLAN.md`: plan, status, milestone graph, task ownership, risk register.
- `AGENTS.md`: repo-local instructions for agents.
- `WORKER_PROTOCOL.md`: worker rules and handoff format.
- `SPEC_UPDATE_PROTOCOL.md`: spec-management rules.
- `TESTING_STRATEGY.md`: test layers, fixtures, commands, gates.
- `docs/architecture.md`: package/runtime/state/event/stage architecture.
- `docs/contracts.md`: OpenAPI, event, state, config, MCP, CLI, artifact contracts.
- `docs/SECURITY.md`: threat model, auth, tool policy, prompt injection, path safety, secret handling.
- `docs/API.md`: human-readable HTTP/SSE/MCP guide derived from OpenAPI.
- `docs/configuration.md`: config schema, defaults, precedence, profiles.
- `docs/OPERATING.md`: running, troubleshooting, release/smoke guidance.
- Spec coverage matrix in `DEVPLAN.md` or later `docs/spec-coverage.md`.

Do not create all secondary docs at once unless their subject exists. Create docs at the milestone where behavior becomes real, then maintain them continuously.

Documentation acceptance per milestone:
- Docs mention only implemented behavior or clearly mark planned behavior.
- Docs link to the canonical spec for requirements.
- Docs list commands that are valid for the current repo or clearly marked recommended/future.
- Contracts docs match machine contracts.
- Testing docs match actual fixtures/commands.
- Security docs are complete before security-sensitive features are accepted.

## 14. Spec Coverage Matrix

This matrix is updated as implementation proceeds. M0 and M1 first-wave evidence is reflected below; later feature behavior remains planned or in progress as noted.

| Requirement ID | Spec Area | Requirement Summary | Milestone | Owner | Implementation Path | Test Coverage | Docs | Status | Notes |
|---|---|---|---|---|---|---|---|---|---|
| PROD-001 | Product | Local-first Go single-binary coding harness | M0-M19 | Foundation | `cmd/nexdev`, imported geoffrussy CLI base | `go test ./...`, `go test -race ./...`, `go vet ./...`, `go mod verify`, `govulncheck ./...`, fake-provider E2E, release check | README, architecture, release readiness | in_progress | Core local-first fake-provider path, control plane, MCP, CLI, TUI, auth, artifacts, and handoff exist through M19. Release gates now pass under Go `1.26.4`. Full real-provider run execution, shell verify runner, and generated API checks remain follow-up. |
| BASE-001 | Source synthesis | Fork/import geoffrussy and preserve provider/state/navigation/executor/git | M0 | Foundation | repo-wide imported source | `go test ./...`, `go vet ./...` | architecture | verified | Imported geoffrussy at `e29f8e7649584585a93d8fc8ac9123036fcaf38e`; planning docs preserved |
| STAGE-001 | Stage graph | Implement canonical stage graph and prerequisites | M1/M5 | Pipeline | `internal/pipeline` | `go test ./internal/pipeline`; `go test ./...` | architecture; contracts; testing | verified | M1-C3 verified contract constants, status transitions, prerequisite snapshot rules, and import-cycle-safe interfaces; M5 verified durable runner registration, prerequisite integration, status persistence, resume selection, checkpoint output/error JSON, and persisted events. Concrete product stages remain M6/M7 |
| STATE-001 | SQLite | Durable SQLite state with WAL/FK/busy timeout | M3 | State | `internal/state` | `go test ./internal/state`; integration tests later | contracts; testing | verified | M1-C5 migration skeleton verified; M3 event, run/stage/artifact, auth token, steering, detour, navigation, and plan edit repositories verified; higher-level service integrations remain later milestones |
| EVENT-001 | SSE/events | Persisted event log, monotonic per run | M3/M10/M16/M19 | State/Control | `internal/state/events.go`, `internal/controlplane/events.go`, `internal/controlplane/sse.go`, `scripts/e2e_fake_provider.sh` | `go test ./internal/state`; `go test ./internal/controlplane`; `./scripts/e2e_fake_provider.sh`; `./scripts/release_check.sh` | contracts/API; testing | in_progress | M3/M10 implement persisted event log, publisher persist-before-broadcast, SSE frames, heartbeat comments, bounded queues, and `Last-Event-ID` replay. M16 E2E validates replay over served control plane. M19 adds package-level slow-client publisher overflow coverage and release gates pass; full HTTP slow-reader stress remains follow-up. |
| API-001 | Control plane | Required HTTP routes from OpenAPI | M1/M10/M12/M17 | Control/App | `api/openapi.yaml`, `internal/controlplane`, `internal/app`, `internal/cli` | `go test ./internal/contract`; `go test ./internal/controlplane`; `go test ./internal/app ./internal/cli`; generated-code validation later | contracts/API | in_progress | OpenAPI skeleton and role metadata exist. M10 manually binds all required routes with stdlib ServeMux, implements durable read routes plus detour/control adapters, and returns contract-shaped errors. M12 wires `nexdev serve` to the M10/M11 server with project-local state, safe bind/auth config, and project lock lifecycle. M17 wires provider-test execution only through an injected service under explicit real-provider smoke env gates. Generated OpenAPI server types, full response validation, task/config mutation services, general provider mutation, and full run starter remain follow-up. |
| AUTH-001 | Auth | Opaque bearer tokens with roles/expiry/revocation | M3/M10/M12/M15 | Auth/App/CLI | `internal/controlplane/auth.go`; `internal/state/auth_tokens.go`; `internal/controlplane/routes.go`; `internal/app/lifecycle.go`; `internal/cli/m12_commands.go` | `go test ./internal/controlplane`; `go test ./internal/state`; `go test ./internal/app ./internal/cli` | security/contracts | implemented | M3 token repository stores hash-only role/name/expiry/revocation/last-used metadata. M10 adds random opaque token generation helper, HMAC-SHA256 hashing with server secret, constant-time middleware lookup, expiry/revocation rejection, role enforcement, successful-auth `last_used_at` touch, route role parity with OpenAPI, and remote-bind-without-auth startup rejection. M12 adds project-local `auth token create|list|revoke` UX with plaintext returned only on create. M15 adds local auth throttling and audit records for throttled attempts. |
| MCP-001 | MCP | MCP tools as thin wrappers over control plane | M11 | MCP | `internal/controlplane/mcp.go`, `api/mcp_tools.json`, `internal/mcp` legacy registration guard | `go test ./internal/controlplane`; `go test ./internal/mcp` | contracts/API; architecture; testing | implemented | M11 defines static required tool descriptors, per-tool role enforcement, strict input validation, durable state read surfaces, and delegation to `RunStarter`, `executor.Control`, `DetourRequester`, blocker state, and optional `ProviderTester`. M17 wires provider-test execution only when a tester is injected by app env gates; absent testers return structured service errors. Legacy stdio registration is disabled to avoid provider/executor bypass. Artifact content reads and safe stdio adaptation remain follow-up. |
| REPO-001 | Repo analysis | Deterministic bounded repository analysis artifact | M6 | Pipeline | `internal/pipeline/repo_analyze.go` | `go test ./internal/pipeline`; full pipeline integration later | architecture; contracts; testing | implemented | M6-REPO-ANALYZE produces `contract.RepoAnalysis`, excludes secret/runtime/dependency/build paths, captures untrusted repo instructions as strings with risk notes, writes `.nexdev/artifacts/repo_analysis.json`, and indexes the artifact when `*state.Store` plus project/run IDs are available; shared artifact helper and `artifact_updated` event emission remain M7/M10 follow-up |
| PROVIDER-001 | Provider | Preserve registry and route by stage | M1/M4/M16/M17 | Provider | `internal/provider/router.go`; `internal/provider/structured.go`; `internal/provider/fake.go`; `internal/provider/real_smoke.go`; imported provider registry | `go test ./internal/provider`; `go test ./internal/app ./internal/cli`; `./scripts/e2e_fake_provider.sh`; optional `./scripts/real_provider_smoke.sh` | architecture; contracts; testing; README; security | in_progress | M1-C6 verified slot routing and primary inheritance; M4 structured wrapper and fake provider verified; M16 wires explicit constructor-only fake-provider E2E without global registry mutation. M17 adds disabled-by-default real-provider smoke gates and provider-test service delegation. Raw response persistence remains follow-up; real-provider full pipeline execution remains out of scope. |
| INTERVIEW-001 | Interview | Provider-backed structured requirements artifact | M6 | Pipeline | `internal/pipeline/interview.go` | `go test ./internal/pipeline`; full pipeline integration later | architecture; contracts; testing | implemented | M6-INTERVIEW-COMPLEXITY uses `StructuredClient`/`SlotInterview`, blocks unresolved open questions outside yes/CI, converts yes/CI questions to `Assumption:` constraints, writes `.nexdev/artifacts/interview.json`, and indexes when state is available |
| COMPLEXITY-001 | Complexity | Deterministic/provider-refined complexity profile | M6 | Pipeline | `internal/pipeline/complexity.go` | `go test ./internal/pipeline`; full pipeline integration later | architecture; contracts; testing | implemented | M6-INTERVIEW-COMPLEXITY computes deterministic complexity first, optionally refines through `SlotComplexity`, enforces deterministic floors for score/level/phases/risks/voices/tests, writes `.nexdev/artifacts/complexity_profile.json`, and indexes when state is available |
| STRUCT-001 | Structured outputs | Strict decode, semantic validate, repair/reject | M4/M6 | Provider/Pipeline | `internal/contract/model_outputs.go`; `internal/provider/structured.go`; `internal/pipeline/repo_analyze.go`; `internal/pipeline/interview.go`; `internal/pipeline/complexity.go`; `internal/pipeline/design.go`; `internal/pipeline/hivemind.go`; `internal/pipeline/validate.go` | `go test ./internal/contract`; `go test ./internal/provider`; `go test ./internal/pipeline`; broader pipeline integration later | contracts; architecture; testing | implemented | Provider wrapper now strict-decodes, rejects unknown fields, validates semantically, repairs/rejects with capped attempts, and returns raw/usage metadata; M6 repo_analyze deterministically produces `contract.RepoAnalysis`; M6 interview/complexity/design/hivemind/validate consume `StructuredClient` with fake-provider repair coverage; raw provider-response persistence remains state/observability follow-up |
| HIVEMIND-001 | Hivemind | Provider-backed multi-voice critique and synthesis artifact | M6 | Pipeline | `internal/pipeline/hivemind.go` | `go test ./internal/pipeline`; full pipeline integration later | architecture; contracts; testing | implemented | M6-HIVEMIND-VALIDATE uses `SlotHivemindVoice` and `SlotHivemindSynthesis`, configured voices, sequential default, bounded parallel option, design review artifact writing/indexing, structured repair coverage, and blocks on synthesis revise/block after writing review |
| VALIDATE-001 | Validation | Provider-backed pre-plan sanity report | M6 | Pipeline | `internal/pipeline/validate.go` | `go test ./internal/pipeline`; full pipeline integration later | architecture; contracts; testing | implemented | M6-HIVEMIND-VALIDATE uses `SlotValidate`, writes validation report and validated design for pass/warn, blocks conflicts/blockers/block verdicts by default, and keeps requirements intact in prompt contract |
| PLAN-001 | Planning | Generate phase sketches, detailed task DAG, and anchored plan artifacts | M7 | Pipeline | `internal/pipeline/plan_sketch.go`; `internal/pipeline/plan_detail.go`; `internal/pipeline/plan_validation.go` | `go test ./internal/pipeline`; full pipeline integration later | architecture; contracts; testing | implemented | M7 produces canonical phase IDs/numbers, validates task acceptance criteria, expected files, dependency references, and dependency cycles, renders deterministic `devplan.json`, `devplan.md`, and `phaseNNN.md`, indexes artifacts, and persists pending `nexdev_tasks`; parallel file-overlap assignment remains later executor/worktree work |
| REVIEW-001 | Review | Gate plan before develop with approval, edits, versioning, and audit events | M7 | Pipeline | `internal/pipeline/review.go` | `go test ./internal/pipeline`; control-plane/TUI integration later | architecture; contracts; testing | implemented | M7 supports manual/auto/ci/explicit skip modes, writes approval marker `.nexdev/artifacts/review_approval.json` with `reviewed_approved_plan`, rejects unsafe non-pending edits, increments plan versions, and writes `plan_edit_events`; review task update/delete currently use direct SQL through `Store.DB()` pending state repository methods and must stay internal until repository follow-up |
| EXEC-001 | Develop executor | Safe fake-worker develop bridge over approved tasks | M8 | Executor/Pipeline | `internal/executor/nexdev.go`; `internal/pipeline/develop.go` | `go test ./internal/executor ./internal/pipeline`; `go test ./...`; `go vet ./...`; `go mod verify` | architecture; contracts; testing | verified | M8 loads reviewed `nexdev_tasks`, requires the review approval marker, persists task status/events, creates blockers, rejects unexpected writes through expected-file/path checks, and exposes in-process pause/resume/cancel/skip/current-task controls. Real LLM execution, shell/network tools, control-plane/CLI wiring, project-lock lifecycle, changed-file manifests, and full artifact-backed prompt context remain follow-up work |
| SECURITY-001 | Tool/path safety | Deny shell by default, path/symlink policy | M2/M15 | Security | `internal/config`, `internal/safety`, `internal/git`, `internal/pipeline`, `internal/controlplane`, `internal/observability` | `go test ./internal/config ./internal/safety`, `go test ./internal/safety`, `go test ./internal/git`, `go test ./internal/controlplane`, `go test ./internal/pipeline`, `go test ./internal/observability`; E2E fixtures later | architecture; contracts; testing; security | in_progress | M2 config/path, security helper, and project lock baselines verified. M15 adds expected-file/file-lock helper checks, stale-lock safe failure, artifact/prompt/API-error redaction coverage, Hivemind `security_warning` events, auth throttling/audit, and cost preflight denial. Shell/network execution remains unimplemented; tool policy file loading, verify command runner enforcement, slow-client stress, and full E2E no-leak fixtures remain follow-up. |
| LOCK-001 | Runtime paths | Single mutating process project lock | M2/M12/M15 | Git/App/Security | `internal/git/project_lock.go`; `internal/app/lifecycle.go` | `go test ./internal/git`; `go test ./internal/app`; race/multi-process lifecycle tests later | architecture; contracts; testing; security | implemented | `.nexdev/run/project.lock` helper resolves through path sanitizer, acquires with exclusive create, writes pid/timestamp metadata, rejects held locks, and releases by removing the file. M12 wires acquire/release around `nexdev serve`; M15 adds deterministic stale-lock detection with safe failure and manual recovery instead of unsafe process probing/deletion. |
| CLI-001 | CLI | Operator command surface and global flags | M12/M16/M17 | CLI/App | `cmd/nexdev`, `internal/cli`, `internal/app` | `go test ./internal/app ./internal/cli`; `./scripts/e2e_fake_provider.sh`; optional `./scripts/real_provider_smoke.sh` | README; architecture; contracts; testing | in_progress | M12 switches root identity to `nexdev`, adds required global flags, wires `serve`, token UX, local/remote status/events/provider/artifact reads, doctor, and HTTP client adapters for pause/resume/cancel/steer/detour/blockers/provider-test. M13 adds `tui`. M16 wires local `run --fake-provider --no-tui --json` through the app runner. M17 injects provider-test execution only under explicit real-provider smoke env gates. Live event follow, standalone verify/handoff commands, and real-provider run wiring remain follow-up. |
| TUI-001 | TUI | Terminal control UX as a client of core API/state/control services | M13 | TUI/CLI | `internal/tui/nexdev.go`; `internal/cli/m12_commands.go` | `go test ./internal/tui`; `go test ./internal/cli`; broader gates later | README; architecture; contracts; testing | implemented | M13 adds terminal-only `nexdev tui`, client/service snapshot model, overview/events/plan/blocker/artifact-config-provider views, refresh/navigation/control keybindings, disabled/deferred action states, redacted rendering, and quit/confirmation safety. It does not create web UI assets, call providers, execute shell commands, or mutate pipeline state directly. |
| OBS-001 | Observability | Structured logs, disabled-by-default OTel, audit records, provider usage/cost ledger, and redaction boundaries | M2/M14/M15 | Observability | `internal/observability`; `internal/state/audit_cost.go`; `internal/provider/structured.go`; `internal/controlplane/auth.go` | `go test ./internal/observability`; `go test ./internal/state ./internal/provider ./internal/controlplane ./internal/config ./internal/app`; full no-leak/E2E later | architecture; contracts; testing | implemented | M2 logging baseline verified with JSON/text construction, level filtering, redacted messages/attrs, and field helpers. M14 adds audit/cost schema/repos, event JSON scrubbing, provider structured-call usage recorder hook, disabled OTel config helper, typed cost/observability config defaults, auth/control request audit records, app provider-recorder wiring, and targeted no-secret tests. M15 adds deterministic cost guard preflight denial before configured provider launches. Run-summary aggregation, OTel exporters, and broad E2E no-leak fixtures remain follow-up. |
| STEER-001 | Steering | Durable steering events influence prompt context only | M3/M8 | State/Executor | `internal/steering/contracts.go`; `internal/state/steering.go`; `internal/executor/nexdev.go` | `go test ./internal/steering`; `go test ./internal/state`; `go test ./internal/executor ./internal/pipeline` | contracts; testing | verified | M8 persists steering events, emits `steering_added`, selects last-N task-scoped steering messages plus latest summary into prompt context, and preserves `SafetyPolicyOverrideAllowed=false`. Artifact-backed requirements/design/repo context and model/deterministic summarization beyond stored summaries remain follow-up |
| DETOUR-001 | Detour | Blocker/manual detour creates minimal spliced tasks | M3/M9/M10/M12 | State/Detour/Control/CLI/TUI | `internal/detour/contracts.go`; `internal/state/detours.go`; `internal/state/tasks.go`; `internal/detour/manager.go`; `internal/detour/splice.go`; `internal/controlplane/routes.go`; `internal/app/lifecycle.go`; `internal/cli/m12_commands.go` | `go test ./internal/detour`; `go test ./internal/state`; `go test ./internal/controlplane`; `go test ./internal/app ./internal/cli`; TUI tests later | architecture/contracts; testing | verified | Core M9 provider-backed generation, validation, dense/gapped immediate splice, depth-exceeded blocker, detour record persistence, trigger `pending_after_detour`, and `detour_created` event are package-verified. Dense plans are accepted through state-owned transactional insertion that shifts later tasks within the same run and plan version. M10 adds authenticated operator HTTP delegation to an M9-compatible requester without provider calls or ad hoc ordering. M12 wires the app server to the M9 workflow manager and adds a CLI remote detour adapter; TUI preview/control UX remains later work. |
| VERIFY-001 | Verify | Policy-gated verification commands with repair loop | M7/M16 | Pipeline | `internal/pipeline/verify_handoff.go` | `go test ./internal/pipeline`; E2E | testing | in_progress | M16 writes `verify_report.json`, persists verify started/completed events, and reports configured commands as policy-denied without executing them. Real policy-gated command execution, output caps, and repair loop remain follow-up. |
| HANDOFF-001 | Handoff | Write handoff, changed files, run summary | M7/M16 | Pipeline/Git | `internal/pipeline/verify_handoff.go`; artifacts | `go test ./internal/pipeline`; E2E | README/operating | in_progress | M16 writes `changed_files.json`, `run_summary.json`, and `handoff.md` from expected-file manifests and redacts request text. Git diff anchoring and standalone command UX remain follow-up. |
| TEST-001 | Testing | Fake-provider full pipeline and security tests | M16/M19 | Test Infra | `internal/testutil`; `internal/provider/fake.go`; `scripts/e2e_fake_provider.sh`; `scripts/release_check.sh` | `go test ./internal/testutil`; `go test ./internal/provider`; `go test ./internal/app ./internal/cli ./internal/pipeline`; `./scripts/e2e_fake_provider.sh`; `go test -race ./...`; `go vet ./...`; `go mod verify`; `govulncheck ./...`; `./scripts/release_check.sh` | testing; release readiness | in_progress | M1-C9 verified shared fixture contracts; M4 verified provider-owned fake provider; M16 adds fake-provider full pipeline/E2E script, changed-file/artifact checks, SSE replay validation, and fixture secret no-leak scan. M19 adds the release gate script and package-level slow-client publisher overflow coverage. Full gates pass under Go `1.26.4`; broader hostile security fixtures and full HTTP slow-reader stress remain follow-up. |
| DOC-001 | Docs | Docs and spec updated continuously | M0-M19 | Docs/Spec | docs | review; `./scripts/release_check.sh` | all docs | implemented | M18 reconciles README, architecture, contracts, API guide, security, testing strategy, spec-update changelog, milestone status, and coverage through M17 without changing `SPEC.md`. M19 adds final release handoff/operations details and final spec-management changelog after release gates pass. |

## 15. Risk Register

### Technical Risks

| Risk | Impact | Mitigation | Owner |
|---|---|---|---|
| Repo is not yet geoffrussy fork | Spec assumptions may not match files | M0 import/fork decision gate | Foundation |
| Import cycles around `StageEnv` | Blocks compile and parallel work | Use small interfaces and contract package boundaries | Contract/Pipeline |
| API/state/event schema drift | Control surfaces disagree | OpenAPI/codegen and contract tests first | Contract/API |
| SQLite lock contention | Flaky event/control behavior | Single writer discipline, WAL, busy timeout, short transactions | State |
| SSE replay race | Lost or reordered events | Persist-before-broadcast and sequence tests | State/Control |
| Structured model parsing fragility | Invalid artifacts accepted | Strict decode, semantic validation, repair cap | Provider/Pipeline |
| Fake provider under-specification | CI passes while real flow broken | Rich fixtures for valid/repairable/unrecoverable cases | Test Infra |
| Verification command runner unsafe | Commands can hang or exfiltrate | Policy gate, timeout, output cap, controlled env | Security/Pipeline |

### Orchestration Risks

| Risk | Impact | Mitigation | Owner |
|---|---|---|---|
| Workers edit outside ownership | Merge conflicts and architecture drift | Worker protocol and file boundaries | Orchestrator |
| Feature work starts before contracts | Rework and import cycles | M1 contract freeze gate | Orchestrator |
| Subagents continue through blockers | Hidden hacks | Mandatory blocker/deblocker workflow | Orchestrator |
| Docs/spec lag implementation | Future workers use stale guidance | Spec-management after each milestone | Spec Management |
| Too many workers edit same files | Merge conflicts | Split by package/file and serialize shared files | Orchestrator |

### Security Risks

| Risk | Impact | Mitigation | Owner |
|---|---|---|---|
| Prompt injection from repo docs/tests | Safety policy bypass | Trusted/untrusted prompt sections and security fixtures | Security |
| Tool poisoning via MCP descriptions | Permission escalation | Static schemas and deterministic policy checks | MCP/Security |
| Secret leakage in prompts/logs/events | Credential exposure | Central redaction and deny `.env` model context | Security |
| Remote bind without auth | LAN compromise | Startup fail unless explicit auth | Auth/Control |
| Symlink/path traversal writes | Filesystem escape | Path sanitizer and symlink tests | Safety |
| Unbounded spend | Cost blowout | Cost guards before hivemind/parallel calls | Provider/Observability |

### Cost/Provider Risks

| Risk | Impact | Mitigation | Owner |
|---|---|---|---|
| Real provider tests run by default | Cost/secrets leak | Opt-in env vars only | Provider/Test Infra |
| Unknown price models | Bad cost estimates | Warn in dev, stricter in CI/profile | Observability |
| Parallel hivemind exceeds cap | Cost blowout | Cost guard before launch | Pipeline/Provider |
| Provider-specific assumptions | Vendor lock-in | Preserve provider interface and router | Provider |

### Parallel-Merge Risks

| Risk | Impact | Mitigation | Owner |
|---|---|---|---|
| Multiple workers edit `api/openapi.yaml` | Conflicts/drift | Contract/API Worker owns, others request changes | Orchestrator |
| Multiple workers edit migrations | Ordering conflicts | State Worker serializes migration numbers | State |
| Pipeline stage workers edit runner | Framework breakage | Runner owned by Pipeline Framework Worker | Pipeline |
| CLI workers edit root command concurrently | Conflicts | Root command serialized; command groups own subfiles | CLI |
| Docs become contradictory | Bad handoff | Docs Worker reconciles before milestone acceptance | Docs |

## 16. Completion Definition

Nexdev v0.1 is done only when:
- All devplan milestones are complete or explicitly deferred with orchestrator-approved spec updates.
- Every canonical spec requirement is implemented or explicitly deferred in `SPEC.md`.
- Spec coverage matrix is complete.
- Docs are updated and match behavior.
- `go test ./...` passes.
- `go test -race ./...` passes or documented package-level exclusions are approved.
- `go vet ./...` passes.
- `govulncheck ./...` passes.
- `go mod verify` passes.
- Fake-provider E2E pipeline passes.
- Security fixture tests pass.
- Control-plane/API contracts are tested.
- SSE replay tests pass.
- Auth role matrix passes.
- No unresolved blockers remain.
- Final handoff exists for future maintainers.

## 17. Prompt for the Separate Nexdev Development Session

Paste the following prompt into a fresh OpenCode session to begin implementation.

```markdown
You are ChatGPT 5.5 acting as the Nexdev Build Orchestrator inside OpenCode.

Your job in this session is to build Nexdev from the canonical specification and devplan using subagents only for implementation work. You, the orchestrator, must not implement production code directly.

Before doing anything else:
1. Read `SPEC.md` completely.
2. Read `DEVPLAN.md` completely.
3. Read `AGENTS.md`.
4. Read `WORKER_PROTOCOL.md`.
5. Read `SPEC_UPDATE_PROTOCOL.md`.
6. Read `TESTING_STRATEGY.md`.
7. Read `docs/architecture.md` and `docs/contracts.md`.

Source-of-truth order:
1. `SPEC.md`.
2. Generated and documented contracts: `api/openapi.yaml`, generated API types, `docs/contracts.md`, migrations, schemas.
3. `DEVPLAN.md`.
4. Test fixtures and schemas.
5. Implementation.

If implementation disagrees with `SPEC.md`, the implementation is wrong unless the Spec Management Subagent has updated `SPEC.md` with orchestrator approval.

Operate as an orchestrator:
- Use subagents for every implementation change.
- Use parallel subagents wherever file ownership makes it safe.
- Never implement production code directly as the orchestrator.
- Keep workers inside ownership boundaries from `DEVPLAN.md`.
- Require workers to read relevant spec sections, `DEVPLAN.md`, `WORKER_PROTOCOL.md`, and docs before editing.
- Require tests and docs with every behavior change.
- Require every worker to update `DEVPLAN.md` progress and return a handoff note.
- Require every worker handoff to include updated suggested next actions, including the next recommended task, dependencies, blockers, and any documentation follow-up.
- Whenever documentation is updated, require the worker to update the relevant "next actions" or "next recommended task" guidance in `DEVPLAN.md`, the changed doc, or the handoff so the next orchestrator has current instructions.
- Spawn a Spec Management Subagent after every major milestone.
- Spawn specialized Deblocker Subagents when blocked.
- Do not allow workers to guess or hack around blockers.
- Stop only when `DEVPLAN.md` and `SPEC.md` are fully complete or when an unrecoverable orchestrator decision is required.

Current repository context from planning:
- At planning time, the repository was greenfield and contained only `SPEC.md` plus planning docs.
- The canonical spec requires a Go 1.24+ implementation and repository strategy of forking/importing `mojomast/geoffrussy`.
- Preserve root `SPEC.md` and planning docs during bootstrap.

Recommended first wave of builder subagents:

1. Foundation Worker
Goal: Execute M0 bootstrap. Determine whether `mojomast/geoffrussy` can be forked/imported into this repository, preserve planning docs, initialize/confirm Git and Go module, and establish baseline test command.
Owned files: `go.mod`, `go.sum`, `.gitignore`, initial build/tool files, minimal app entrypoint only if needed.
Must not implement product features.

2. Contract/API Worker
Goal: Start M1 by creating `api/openapi.yaml` skeleton for all required routes, `ErrorResponse`, status/plan/artifact/event schemas, auth role metadata, and codegen plan.
Owned files: `api/openapi.yaml`, generated API path once decided, `docs/contracts.md` API sections.
Depends on Foundation Worker module/tooling decisions.

3. State Worker
Goal: Start M1/M3 state contract planning after bootstrap. Define migration numbering, initial state models, and SQLite setup plan without racing OpenAPI contracts.
Owned files: `internal/state/*`, `internal/state/migrations/*`, state sections of `docs/contracts.md`.
Depends on Foundation Worker and contract constants.

4. Pipeline Framework Worker
Goal: Start M1/M5 stage contracts. Define canonical stage names, statuses, prerequisite matrix, `PipelineStage`, and small interfaces that avoid import cycles.
Owned files: `internal/pipeline/stage.go`, `internal/pipeline/navigation.go`, pipeline contract tests, architecture stage docs.
Depends on Foundation Worker.

5. Test Infrastructure Worker
Goal: Create initial black-box test helpers and testing conventions after module bootstrap. Prepare fake ID/time/provider fixture contracts.
Owned files: `internal/testutil/*`, `TESTING_STRATEGY.md`, initial CI/test helper files.
Depends on Foundation Worker.

6. Documentation Worker
Goal: Keep `README.md`, `docs/architecture.md`, and `docs/contracts.md` aligned with actual bootstrap and first contracts. Do not duplicate the full spec.
Owned files: docs only.
Can run in parallel with contract workers after reading their handoffs.

First orchestrator checkpoint:
- Wait for Foundation Worker to finish M0 bootstrap.
- Inspect worker handoff, tests run, and files changed.
- Confirm the handoff includes current suggested next actions before launching follow-up work.
- If geoffrussy was not imported, spawn Spec Management Subagent before proceeding.
- Then launch M1 contract workers in parallel with strict file ownership.

Blocker workflow:
If any worker hits a blocker, it must stop and return a blocker handoff with exact error/output and reproduction steps. Spawn the specialized Deblocker Subagent named in `DEVPLAN.md`. Continue only after accepting the deblocker report.

Completion target:
Work milestone by milestone through M19. The project is complete only when all devplan milestones are complete, every spec requirement is implemented or explicitly deferred in `SPEC.md`, tests pass, docs match behavior, spec coverage is complete, fake-provider E2E passes, security checks pass, API/SSE/auth contracts are tested, and no unresolved blockers remain.

Handoff rule:
When the user asks for a handoff, or when a worker updates docs, ensure suggested next actions are updated at the same time. The next actions must be specific, current, and tied to `DEVPLAN.md` task IDs or milestone IDs where possible.

Make it go brrrr, but make it merge cleanly.
```
